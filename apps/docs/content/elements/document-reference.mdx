---
title: "文档引用"
description: "答案所依据的文档，包含引用段落和可跳转的页面。"
---

DocumentReference 是一个用于单个源文档的引用卡片：顶部显示文件名，下面列出可在其间切换的页面锚点。在运行时中，这些锚点来自读取文档的工具调用；独立使用时，你已经持有这些锚点。

## 入门

<RuntimeMode>

assistant-ui 内置的 `source` 消息部分支持单个文档引用（标题、文件名），但不支持带引用段落的页面锚点列表。最接近这种结构的实际构建块是工具调用：让模型读取文档并返回其锚点，然后渲染结果。

<Steps>
  <Step>

### 渲染工具调用

```tsx title="components/assistant-ui/elements/read-document-tool-ui.tsx"
"use client";

import { useState } from "react";
import type { ToolCallMessagePartComponent } from "@assistant-ui/react";
import {
  DocumentReference,
  type DocumentAnchor,
} from "@/components/assistant-ui/elements/document-reference";

type ReadDocumentResult = {
  title: string;
  pages: number;
  anchors: DocumentAnchor[];
};

export const ReadDocumentToolUI: ToolCallMessagePartComponent<
  { query: string },
  ReadDocumentResult
> = ({ result }) => {
  const [activePage, setActivePage] = useState<number>();

  if (!result) return null;
  const page = activePage ?? result.anchors[0]?.page ?? 1;

  return (
    <DocumentReference
      title={result.title}
      pages={result.pages}
      anchors={result.anchors}
      activePage={page}
      onJump={setActivePage}
    />
  );
};
```

  </Step>
  <Step>

### 注册工具

```tsx title="app/toolkit.ts"
import { defineToolkit } from "@assistant-ui/react";
import { z } from "zod";
import { ReadDocumentToolUI } from "@/components/assistant-ui/elements/read-document-tool-ui";

export const toolkit = defineToolkit({
  read_document: {
    type: "frontend",
    description: "Open a document and return the passages that answer the question.",
    parameters: z.object({ query: z.string() }),
    execute: async ({ query }) => lookupDocument(query),
    render: ReadDocumentToolUI,
  },
});
```

```tsx title="app/MyRuntimeProvider.tsx"
import { AssistantRuntimeProvider, AuiConfig, Tools } from "@assistant-ui/react";
import { useChatRuntime } from "@assistant-ui/ai-sdk";
import { toolkit } from "./toolkit";

export function MyRuntimeProvider({ children }: { children: React.ReactNode }) {
  const runtime = useChatRuntime();
  const config = AuiConfig({ tools: Tools({ toolkit }) });
  return (
    <AssistantRuntimeProvider runtime={runtime} config={config}>
      {children}
    </AssistantRuntimeProvider>
  );
}
```

请参阅[工具 UI](/docs/tools/tool-ui)，了解由后端定义的工具和审批门。

  </Step>
</Steps>

</RuntimeMode>

<StandaloneMode>

独立使用时，该元素完全受控：你持有锚点列表和当前页面，并由它将跳转操作报告回你的代码。

<Steps>
  <Step>

### 保存当前页面

```tsx title="app/citation.tsx"
"use client";

import { useState } from "react";
import { DocumentReference } from "@/components/assistant-ui/elements/document-reference";

const ANCHORS = [
  { page: 4, quote: "Composer state moved off the thread runtime in 0.14." },
  { page: 9, quote: "Existing drafts migrate on first read; nothing to run by hand." },
];

export function Citation() {
  const [activePage, setActivePage] = useState(ANCHORS[0].page);
  return (
    <DocumentReference
      title="migration-0.14.md"
      pages={12}
      anchors={ANCHORS}
      activePage={activePage}
      onJump={setActivePage}
    />
  );
}
```

  </Step>
  <Step>

### 替换锚点列表

打开其他文档会替换整个锚点集合，并将当前页面重置为第一个锚点。

```tsx
async function openDocument(id: string) {
  const doc = await fetchDocument(id);
  setTitle(doc.title);
  setPages(doc.pages);
  setAnchors(doc.anchors);
  setActivePage(doc.anchors[0]?.page ?? 1);
}
```

  </Step>
</Steps>

</StandaloneMode>

## 结构

```tsx
<div data-slot="document-reference">
  <div>
    <span>{/* file icon in a tinted square */}</span>
    <div>
      <span>{title}</span>
      <span>{/* "N pages · M cited" */}</span>
    </div>
  </div>
  <div>
    {/* one button per anchor: "p. N" above the quoted passage */}
  </div>
</div>
```

`activePage` 决定两个相互独立的行为。所有 `page` 等于 `activePage` 的锚点都会获得活动背景色；当多个锚点引用同一页面时，可能会有多个按钮获得该背景色。由于通过 `anchors.findIndex` 查找当前锚点，因此该页面在数组顺序中的第一个锚点才会获得 `aria-current`。当 `activePage` 与任何锚点都不匹配时，没有按钮会获得这两个状态。点击按钮时，会使用该锚点的 `page` 调用 `onJump`，而不是使用其数组索引，因此调用方在点击后无法区分两个引用同一页面的锚点。空的 `anchors` 数组只会渲染标题，元信息行显示 `0 cited`，且不会显示占位行。

## 示例

### 同一页面上的多个引用

每个页面的锚点不必唯一。两个引用第 4 页的条目在 `activePage` 为 4 时都会高亮；但只有数组中的第一个条目会被辅助技术标记为当前项。

```tsx
const anchors = [
  { page: 4, quote: "The runtime owns branch state; nothing local to track." },
  { page: 4, quote: "Reloading a message creates a sibling branch automatically." },
];
```

### 重新设置卡片样式

两条路径都会在根元素上接收 `className`，该属性读取共享的 `paper` token。当前锚点的背景色读取 `field`，元信息行和每个锚点的页面标签则读取 `mono`；两者都位于 `surfaces.tsx` 中。

```tsx
<DocumentReference className="max-w-none gap-4" /* ... */ />
```

## API 参考

<RuntimeMode>

### 工具调用渲染属性

| 属性 | 类型 | 描述 |
|------|------|-------------|
| `args` | `TArgs` | 解析后的参数。模型仍在流式传输参数时，该值可能不完整。 |
| `argsText` | `string` | 模型流式传输的原始 JSON 参数文本。 |
| `result` | `TResult \| undefined` | 工具完成后返回的值。运行中为 `undefined`。 |
| `status` | `ToolCallMessagePartStatus` | `status.type` 的值为 `"running"`、`"requires-action"`、`"complete"` 或 `"incomplete"`。 |
| `toolName` | `string` | 模型调用的工具名称。 |
| `toolCallId` | `string` | 此次调用的稳定 ID。 |
| `isError` | `boolean \| undefined` | `result` 是否表示工具执行错误。 |

在工具包条目的 `render` 字段上注册渲染器，并使用 `Tools({ toolkit })` 挂载工具包。请参阅[工具 UI](/docs/tools/tool-ui)，了解完整的渲染属性接口，包括 `addResult`、人工工具和审批门。

</RuntimeMode>

<StandaloneMode>

### DocumentReference

| 属性 | 类型 | 默认值 | 描述 |
|------|------|---------|-------------|
| `title` | `string` | 必填 | 标题文本。它会覆盖原生的 `title` 属性，该属性不会被转发到其他属性中。 |
| `pages` | `number` | 必填 | 页面总数，显示在标题元信息行中。不会与 `anchors` 进行交叉校验。 |
| `anchors` | `readonly DocumentAnchor[]` | 必填 | 要按顺序列出的引用。 |
| `activePage` | `number` | 必填 | 要高亮的页码。可能匹配零个、一个或多个锚点。 |
| `onJump` | `(page: number) => void` | | 按钮按下时，使用该锚点的 `page` 调用。 |
| `className` | `string` | | 合并到根元素上。 |

### DocumentAnchor

| 字段 | 类型 | 描述 |
|-------|------|-------------|
| `page` | `number` | 此锚点引用的页码。 |
| `quote` | `string` | 显示在页码下方的引用段落。 |

其他所有 `div` 属性都会转发到根元素，但 `title` 除外，因为它会被组件自身的属性替换。

</StandaloneMode>
