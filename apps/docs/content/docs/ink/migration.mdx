---
title: 从 Web 迁移
description: 使用 React Ink 将现有的 @assistant-ui/react 应用迁移到终端。
---

如果你已经有一个 assistant-ui Web 应用，大部分代码都可以直接迁移。`@assistant-ui/react` 和 `@assistant-ui/react-ink` 之间通过 `@assistant-ui/core` 共享运行时核心——只有 UI 层需要改变。

## 保持不变的部分

- **运行时设置**——`useLocalRuntime`、`ChatModelAdapter` 以及所有运行时选项的工作方式完全相同。
- **AI SDK 集成**——`@assistant-ui/ai-sdk` 可与 React Ink 配合使用。你的运行时设置可以直接迁移。
- **工具定义**——`Tools({ toolkit })` 和工具包渲染器使用相同的 API。
- **状态 Hooks**——`useAuiState`、`useAui` 以及选择器模式保持不变。
- **后端代码**——你的 API 路由、流式传输端点和服务端逻辑无需任何修改。

## 需要改变的部分

| Web（`@assistant-ui/react`） | 终端（`@assistant-ui/react-ink`） |
|---|---|
| `AssistantRuntimeProvider` | `AssistantRuntimeProvider`（名称相同，来自 `@assistant-ui/react-ink`） |
| DOM 原语（`div`、`button`、`input`） | Ink 原语（`Box`、`Text`、`TextInput`） |
| CSS / Tailwind 样式 | Ink 的 flexbox + ANSI 颜色 |
| `@assistant-ui/ui`（shadcn 组件） | 使用原语构建自己的终端 UI |
| `@assistant-ui/react-markdown` | `@assistant-ui/react-ink-markdown`（使用 ANSI 渲染的 Markdown） |

## 分步迁移

<Steps>
  <Step>

### 安装 React Ink 包

<InstallCommand npm={["@assistant-ui/react-ink", "ink", "react"]} />

  </Step>
  <Step>

### 保留你的运行时 Hook

你的 `useLocalRuntime` 设置可以直接使用——无需任何修改：

```tsx title="hooks/use-app-runtime.ts"
// This file is identical to your web version
import { useLocalRuntime, type ChatModelAdapter } from "@assistant-ui/react-ink";

export function useAppRuntime(adapter: ChatModelAdapter) {
  return useLocalRuntime(adapter);
}
```

<Callout type="tip">
  如果你使用 monorepo，可以直接在 Web 项目和终端项目之间共享适配器。
</Callout>

  </Step>
  <Step>

### 替换 Provider

```tsx
// Before (web)
// import { AssistantRuntimeProvider } from "@assistant-ui/react";

// After (Terminal) — same name, different package
import { AssistantRuntimeProvider } from "@assistant-ui/react-ink";

export function App() {
  const runtime = useAppRuntime(adapter);
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      {/* your terminal UI */}
    </AssistantRuntimeProvider>
  );
}
```

  </Step>
  <Step>

### 重建 UI 层

这是迁移工作的主要部分。Web 组件无法在终端中渲染。请将它们替换为 Ink 原语：

```tsx
// Web — DOM-based
// import { Thread } from "@/components/assistant-ui/elements/thread.aui";

// Terminal — use Ink primitives
import {
  ThreadPrimitive,
  ComposerPrimitive,
  MessagePrimitive,
} from "@assistant-ui/react-ink";
import { Box } from "ink";

function MyMessage() {
  return (
    <Box marginBottom={1}>
      <MessagePrimitive.Parts />
    </Box>
  );
}

function ChatScreen() {
  return (
    <ThreadPrimitive.Root>
      <ThreadPrimitive.Messages>
        {() => <MyMessage />}
      </ThreadPrimitive.Messages>
      <Box borderStyle="round" borderColor="gray" paddingX={1}>
        <ComposerPrimitive.Input submitOnEnter placeholder="Message..." autoFocus />
      </Box>
    </ThreadPrimitive.Root>
  );
}
```

请参阅[原语参考](/docs/ink/primitives)，查看所有可用组件的完整列表。

  </Step>
</Steps>

## Monorepo 代码共享

在 monorepo 中，除了 UI 层之外，其他内容都可以共享：

```
packages/
  shared/
    hooks/          ← useAppRuntime with ChatModelAdapter (shared)
    tools/          ← tool definitions (shared)
  web/
    components/     ← DOM-based UI
  terminal/
    components/     ← Ink-based terminal UI
```

运行时 Hook、工具定义和后端代码与平台无关。只有 UI 组件需要分别为 Web 和终端实现。
