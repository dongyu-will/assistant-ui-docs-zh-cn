---
title: 智能组件
description: 通过可读接口和助手工具为 React 组件添加智能能力。
platforms: ["react"]
---

React 通过将逻辑、结构和样式结合在一起的组件彻底改变了 Web 开发。现在，借助 assistant-ui，我们将加入第四个维度：智能。让我们通过一个实际的银行应用示例，学习如何构建智能组件。

## 组件的演进

传统的 React 组件包含三个要素：

```tsx
// Traditional React Component
function TransactionHistory({ transactions }) {
  // 1. Logic (JavaScript/TypeScript)
  const handleRefund = (transactionId) => {
    // Process refund...
  };

  // 2. Structure (JSX/TSX)
  return (
    // 3. Style (CSS via className)
    <div className="transaction-list">
      {transactions.map((transaction) => (
        <div key={transaction.id} className="transaction-item">
          <span>${transaction.amount}</span>
          <span>{transaction.merchant}</span>
          <button onClick={() => handleRefund(transaction.id)}>
            Request Refund
          </button>
        </div>
      ))}
    </div>
  );
}
```

## 添加智能能力

借助 assistant-ui，我们可以使用四个强大的 API 为这个组件增强智能能力：

### 1. 让组件可读（makeAssistantVisible）

首先，让我们的按钮具备“可读性”并支持交互：

```tsx
import { makeAssistantVisible } from "@assistant-ui/react";

// Make the refund button intelligent
const SmartButton = makeAssistantVisible(
  ({ onClick, children }) => <button onClick={onClick}>{children}</button>,
  {
    clickable: true, // Allow the assistant to click the button
  },
);

function TransactionHistory({ transactions }) {
  return (
    <div className="transaction-list">
      {transactions.map((transaction) => (
        <div key={transaction.id} className="transaction-item">
          <span>${transaction.amount}</span>
          <span>{transaction.merchant}</span>
          <SmartButton onClick={() => handleRefund(transaction.id)}>
            Request Refund
          </SmartButton>
        </div>
      ))}
    </div>
  );
}
```

现在，助手可以：

- 理解交易历史的结构
- 与退款按钮交互
- 帮助用户管理交易

### 2. 添加系统指令（useAssistantInstructions）

接下来，让我们向助手提供关于其角色的具体指令：

```tsx
import { useAssistantInstructions } from "@assistant-ui/react";

function SmartTransactionHistory() {
  useAssistantInstructions(`
    You are a helpful banking assistant that:
    1. Helps users understand their transactions
    2. Explains refund policies
    3. Identifies suspicious transactions
    4. Guides users through the refund process
  `);

  return <TransactionHistory transactions={transactions} />;
}
```

### 3. 创建工具

让我们为助手添加交易专用工具：

```tsx title="app/transaction-toolkit.tsx"
"use generative";

import { defineToolkit } from "@assistant-ui/react";
import { z } from "zod";

export default defineToolkit({
  analyzeTransaction: {
    description: "Analyze a transaction's risk and refund eligibility.",
    parameters: z.object({
      transactionId: z.string(),
      merchantName: z.string(),
    }),
    execute: async ({ transactionId, merchantName }) => {
      "use client";
      // Analyze transaction patterns, merchant reputation, etc.
      return {
        isSuspicious: false,
        merchantRating: 4.5,
        similarTransactions: 3,
        refundEligible: true,
      };
    },
    renderText: { running: "Analyzing transaction…", complete: "Analysis ready" },
  },
});
```

```tsx title="app/SmartTransactionHistory.tsx"
import { AuiConfig, AuiProvider, Tools, useAui } from "@assistant-ui/react";
import toolkit from "./transaction-toolkit";

function SmartTransactionHistory() {
  // Previous instructions...
  const aui = useAui();
  const config = AuiConfig({ tools: Tools({ toolkit }) });
  return (
    <AuiProvider extends={aui} config={config}>
      <TransactionHistory transactions={transactions} />
    </AuiProvider>
  );
}
```

### 4. 添加自定义上下文（Model Context）

最后，让我们根据用户的交易模式添加动态上下文：

```tsx
import { useAui } from "@assistant-ui/react";
import { useEffect } from "react";

function SmartTransactionHistory({ userProfile }) {
  const aui = useAui();

  useEffect(() => {
    return aui.modelContext.register({
      getModelContext: () => ({
        system: `
          User spending patterns:
          - Average transaction: ${userProfile.avgTransaction}
          - Common merchants: ${userProfile.frequentMerchants.join(", ")}
          - Refund history: ${userProfile.refundCount} requests
        `,
      }),
    });
  }, [aui, userProfile]);

  // Previous components...
}
```

## 最终效果：智能银行体验

增强后的组件现在可以提供：

- 使用自然语言与交易历史交互
- 提供理解交易的上下文帮助
- 自动分析交易
- 提供智能退款协助

助手现在可以：

1. 读取并理解交易详情
2. 遵循银行业务特定指南
3. 使用工具分析交易
4. 访问用户模式，为用户提供个性化帮助

这样既保留了熟悉的 React 组件模型，又能带来更加直观、安全的银行体验。

## 后续步骤

详细了解各个 API：

- [makeAssistantVisible](make-assistant-visible)，用于理解组件
- [工具](/docs/tools/defining-tools)，用于分析交易
- [useAssistantInstructions](use-assistant-instructions)，用于指导行为
- [Model Context](model-context)，用于管理动态上下文
