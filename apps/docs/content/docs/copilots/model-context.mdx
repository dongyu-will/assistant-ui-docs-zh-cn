---
title: 模型上下文
description: 通过系统指令、工具和上下文提供者配置助手行为。
platforms: ["react"]
---

模型上下文是 assistant-ui 组件智能的基础。它通过上下文提供者系统为助手提供配置和能力。

## 核心概念

### 系统指令

系统指令定义助手的基本行为和可用知识。可以通过多种方式提供：

```tsx
import {
  useAssistantInstructions,
  useAssistantContext,
  makeAssistantVisible,
} from "@assistant-ui/react";

// Static instructions
useAssistantInstructions("You are a helpful assistant...");

// Dynamic context — callback is evaluated lazily at send-time
useAssistantContext({
  getContext: () => `Current page: ${window.location.href}`,
});

// Via makeAssistantVisible
const ReadableComponent = makeAssistantVisible(MyComponent);
// Automatically provides component HTML as system context
```

`useAssistantInstructions` 接受一个发生变化时会重新注册的静态字符串。`useAssistantContext` 接受一个回调，每次读取模型上下文时都会重新执行，因此非常适合注入频繁变化的应用状态，而不会触发重新注册。

### 工具

工具是助手可以用来与应用交互的函数。可以通过多种机制提供：

```tsx title="app/form-toolkit.tsx"
"use generative";

import { defineToolkit } from "@assistant-ui/react";
import { z } from "zod";

export default defineToolkit({
  submitForm: {
    description: "Submit the form with the user's details.",
    parameters: z.object({
      email: z.string().email(),
      name: z.string(),
    }),
    execute: async ({ email, name }) => {
      "use client";
      // Implementation
      return { success: true };
    },
    renderText: { running: "Submitting…", complete: "Submitted" },
  },
});
```

```tsx title="app/Form.tsx"
import {
  AuiConfig,
  AuiProvider,
  makeAssistantVisible,
  Tools,
  useAui,
} from "@assistant-ui/react";
import toolkit from "./form-toolkit";

// Via makeAssistantVisible's clickable option
const ClickableButton = makeAssistantVisible(Button, {
  clickable: true, // Provides a click tool
});

// Use in your component
function Form() {
  const aui = useAui();
  const config = AuiConfig({ tools: Tools({ toolkit }) });
  return (
    <AuiProvider extends={aui} config={config}>
      <form>{/* form fields */}</form>
    </AuiProvider>
  );
}
```

## 上下文提供者系统

上下文提供者系统允许组件向模型上下文贡献内容。以下是一个典型的使用模式：

```tsx
import { useAui, tool } from "@assistant-ui/react";
import { useEffect } from "react";
import { z } from "zod";

// Define tool outside the component (no runtime dependencies)
const myTool = tool({
  parameters: z.object({
    query: z.string(),
  }),
  execute: async ({ query }) => {
    const result = await searchDatabase(query);
    return { result };
  },
});

function MyComponent() {
  const aui = useAui();

  useEffect(() => {
    // Register context provider
    return aui.modelContext.register({
      getModelContext: () => ({
        system: "You are a helpful search assistant...",
        tools: { myTool },
      }),
    });
  }, [aui]); // Re-register if api changes

  return <div>{/* component content */}</div>;
}
```

### 提供者组合

可以注册多个提供者，它们的上下文将被组合：

- 系统指令会进行拼接
- 工具集会进行合并
- 嵌套的可读组件只会在最外层贡献其上下文

## 最佳实践

1. **系统指令**

   - 让指令聚焦于组件的用途，并保持具体明确
   - 使用 useAssistantInstructions 提供显式指令
   - 让 makeAssistantVisible 处理组件结构

2. **工具**

   - 使用 toolkit 条目定义工具的 schema 和行为
   - 对于可复用的工具，优先使用 `Tools({ toolkit })`
   - 妥善处理错误
   - 考虑异步操作和加载状态
   - 尽可能使用内置的 click 工具

3. **上下文管理**
   - 在 useEffect 中注册提供者，以便正确清理
   - 组件卸载时清理提供者
   - 避免深层嵌套的可读组件
   - 考虑大型 HTML 结构对性能的影响
