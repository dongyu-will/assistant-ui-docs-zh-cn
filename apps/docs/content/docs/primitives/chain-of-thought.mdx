---
title: 思维链
description: 用于分组推理步骤和工具调用的可折叠手风琴组件。
platforms: ["react"]
---

import { ChainOfThoughtPrimitiveSample } from "@/components/pages/docs/samples/chain-of-thought-primitive";
import { ChainOfThoughtPrimitiveSample as ChainOfThoughtPrimitiveSampleRadix } from "@/components/pages/docs/samples/chain-of-thought-primitive.radix";
import { Flavored } from "@/components/pages/docs/contexts/flavor.server";
import {
  ChainOfThoughtPrimitive as ChainOfThoughtPrimitiveDocs,
  MessagePrimitive as MessagePrimitiveDocs,
} from "@/generated/primitiveDocs";

ChainOfThought 原语是用于对推理和工具调用部分进行分组的旧版手风琴 API。推理模型会先输出推理 token 和工具调用，然后生成最终答案。

<Callout type="warn">
对于新的分组推理/工具调用 UI，请使用 `MessagePrimitive.GroupedParts`。`ChainOfThoughtPrimitive` 和 `components.ChainOfThought` 仍可用于维护现有代码。
</Callout>

<Tabs items={["Preview", "Code"]}>
  <Tab>
    <Flavored
      base={<ChainOfThoughtPrimitiveSample />}
      radix={<ChainOfThoughtPrimitiveSampleRadix />}
    />
  </Tab>
  <Tab>
```tsx
import { MessagePrimitive, groupPartByType } from "@assistant-ui/react";

function AssistantMessage() {
  return (
    <MessagePrimitive.Root>
      <MessagePrimitive.GroupedParts
        groupBy={groupPartByType({
          reasoning: ["group-chainOfThought", "group-reasoning"],
          "tool-call": ["group-chainOfThought", "group-tool"],
        })}
      >
        {({ part, children }) => {
          switch (part.type) {
            case "group-chainOfThought":
              return <ThinkingAccordion>{children}</ThinkingAccordion>;
            case "group-reasoning":
              return <ReasoningGroup>{children}</ReasoningGroup>;
            case "group-tool":
              return <ToolGroup>{children}</ToolGroup>;
            case "text":
              return <MyText />;
            case "reasoning":
              return <MyReasoning {...part} />;
            case "tool-call":
              return part.toolUI ?? <MyToolFallback {...part} />;
            default:
              return null;
          }
        }}
      </MessagePrimitive.GroupedParts>
    </MessagePrimitive.Root>
  );
}
```
  </Tab>
</Tabs>

## 推荐：GroupedParts

直接在助手消息中对推理和工具调用部分进行分组：

```tsx
import { MessagePrimitive, groupPartByType } from "@assistant-ui/react";

<MessagePrimitive.Root>
  <MessagePrimitive.GroupedParts
    groupBy={groupPartByType({
      reasoning: ["group-chainOfThought", "group-reasoning"],
      "tool-call": ["group-chainOfThought", "group-tool"],
    })}
  >
    {({ part, children }) => {
      switch (part.type) {
        case "group-chainOfThought":
          return <ThinkingAccordion>{children}</ThinkingAccordion>;
        case "group-reasoning":
          return <ReasoningGroup>{children}</ReasoningGroup>;
        case "group-tool":
          return <ToolGroup>{children}</ToolGroup>;
        case "text":
          return <MyText />;
        case "reasoning":
          return <MyReasoning {...part} />;
        case "tool-call":
          return part.toolUI ?? <MyToolFallback {...part} />;
        default:
          return null;
      }
    }}
  </MessagePrimitive.GroupedParts>
</MessagePrimitive.Root>
```

### GroupedParts API 参考

<PrimitivesTypeTable type="MessagePrimitiveGroupedPartsProps" parameters={MessagePrimitiveDocs.GroupedParts.props} />

## 旧版：ChainOfThoughtPrimitive

### 快速开始

使用 `MessagePrimitive.Parts` 渲染普通消息部分；仅当维护已经使用 ChainOfThought 原语的旧代码时，才将 `ChainOfThought` 组件与它们并列放置在同一个 `MessagePrimitive.Root` 中。

<Callout type="info">
运行时设置：原语需要运行时上下文。将 UI 包裹在带有运行时的 `AssistantRuntimeProvider` 中（例如 `useLocalRuntime(...)`）。请参阅[选择运行时](/docs/runtimes/pick-a-runtime)。
</Callout>

### 概念

#### 分组工作方式

`ChainOfThoughtPrimitive.Parts` 从旧版 `components.ChainOfThought` 路径读取当前消息中已分组的推理和工具调用上下文。新代码应改用 `MessagePrimitive.GroupedParts`。

#### 折叠状态

手风琴默认处于折叠状态。`AccordionTrigger` 可在折叠和展开状态之间切换。使用 `AuiIf` 根据折叠状态有条件地渲染部分内容：

```tsx
import { AuiIf, ChainOfThoughtPrimitive } from "@assistant-ui/react";

<ChainOfThoughtPrimitive.Root>
  <ChainOfThoughtPrimitive.AccordionTrigger>
    Thinking
  </ChainOfThoughtPrimitive.AccordionTrigger>
  <AuiIf condition={(s) => !s.chainOfThought.collapsed}>
    <ChainOfThoughtPrimitive.Parts
      components={{ Reasoning }}
    />
  </AuiIf>
</ChainOfThoughtPrimitive.Root>
```

#### Chevron 指示器

使用 `AuiIf` 显示反映当前状态的方向图标：

```tsx
import { AuiIf, ChainOfThoughtPrimitive } from "@assistant-ui/react";
import { ChevronDownIcon, ChevronRightIcon } from "lucide-react";

<ChainOfThoughtPrimitive.AccordionTrigger className="flex w-full cursor-pointer items-center gap-2 px-4 py-2 text-sm">
  <AuiIf condition={(s) => s.chainOfThought.collapsed}>
    <ChevronRightIcon className="size-4" />
  </AuiIf>
  <AuiIf condition={(s) => !s.chainOfThought.collapsed}>
    <ChevronDownIcon className="size-4" />
  </AuiIf>
  Thinking
</ChainOfThoughtPrimitive.AccordionTrigger>
```

#### Parts 组件

`ChainOfThoughtPrimitive.Parts` 接受已弃用的 `components` 属性，用于控制每种部分类型的渲染方式：

```tsx
<ChainOfThoughtPrimitive.Parts
  components={{
    Reasoning: ({ text }) => (
      <p className="whitespace-pre-wrap px-4 py-2 text-muted-foreground text-sm italic">
        {text}
      </p>
    ),
    tools: {
      Fallback: ({ toolName, status }) => (
        <div className="px-4 py-2 text-sm">
          {status.type === "running" ? `Running ${toolName}...` : `${toolName} completed`}
        </div>
      ),
    },
    Layout: ({ children }) => (
      <div className="border-t">{children}</div>
    ),
  }}
/>
```

| 属性 | 类型 | 描述 |
|------|------|-------------|
| `components.Reasoning` | `FC<{ text: string }>` | 渲染推理部分 |
| `components.tools.Fallback` | `ToolCallMessagePartComponent` | 工具调用部分的回退渲染 |
| `components.Layout` | `ComponentType<PropsWithChildren>` | 每个已渲染部分的包装器 |

### Parts API 参考

#### Root

思维链披露 UI 的容器。当未设置 `<div>` 时，渲染一个 `asChild` 元素。

```tsx
<ChainOfThoughtPrimitive.Root className="rounded-lg border">
  ...
</ChainOfThoughtPrimitive.Root>
```

#### AccordionTrigger

用于切换折叠状态的触发器。当未设置 `<button>` 时，渲染一个 `asChild` 元素。

```tsx
<ChainOfThoughtPrimitive.AccordionTrigger className="flex w-full items-center justify-between px-4 py-2 text-sm">
  Thinking
</ChainOfThoughtPrimitive.AccordionTrigger>
```

#### Parts

渲染推理和工具调用部分。此组件不会在内部跟踪折叠状态，因此请按照下面的模式，使用 `AuiIf` 控制可见性。

```tsx
<ChainOfThoughtPrimitive.Parts
  components={{
    Reasoning: ({ text }) => (
      <p className="whitespace-pre-wrap px-4 py-2 text-muted-foreground text-sm italic">
        {text}
      </p>
    ),
    tools: {
      Fallback: ({ toolName, status }) => (
        <div className="px-4 py-2 text-sm">
          {status.type === "running" ? `Running ${toolName}...` : `${toolName} completed`}
        </div>
      ),
    },
  }}
/>
```

<PrimitivesTypeTable type="ChainOfThoughtPrimitivePartsProps" parameters={ChainOfThoughtPrimitiveDocs.Parts.props} />

### 模式

#### 最简手风琴

```tsx
function ChainOfThought() {
  return (
    <ChainOfThoughtPrimitive.Root className="my-2 rounded-lg border">
      <ChainOfThoughtPrimitive.AccordionTrigger className="flex w-full cursor-pointer items-center gap-2 px-4 py-2 font-medium text-sm hover:bg-muted/50">
        Thinking
      </ChainOfThoughtPrimitive.AccordionTrigger>
      <AuiIf condition={(s) => !s.chainOfThought.collapsed}>
        <ChainOfThoughtPrimitive.Parts
          components={{
            Reasoning: ({ text }) => (
              <p className="whitespace-pre-wrap px-4 py-2 text-muted-foreground text-sm italic">
                {text}
              </p>
            ),
          }}
        />
      </AuiIf>
    </ChainOfThoughtPrimitive.Root>
  );
}
```

#### 包含工具调用

```tsx
function ChainOfThought() {
  return (
    <ChainOfThoughtPrimitive.Root className="my-2 rounded-lg border">
      <ChainOfThoughtPrimitive.AccordionTrigger className="flex w-full cursor-pointer items-center gap-2 px-4 py-2 font-medium text-sm hover:bg-muted/50">
        Thinking
      </ChainOfThoughtPrimitive.AccordionTrigger>
      <AuiIf condition={(s) => !s.chainOfThought.collapsed}>
        <ChainOfThoughtPrimitive.Parts
          components={{
            Reasoning: ({ text }) => (
              <p className="whitespace-pre-wrap px-4 py-2 text-muted-foreground text-sm italic">
                {text}
              </p>
            ),
            tools: {
              Fallback: ({ toolName, status }) => (
                <div className="flex items-center gap-2 px-4 py-2 text-sm">
                  <span className="font-medium">{toolName}</span>
                  <span className="text-muted-foreground">
                    {status.type === "running" ? "running..." : "done"}
                  </span>
                </div>
              ),
            },
            Layout: ({ children }) => (
              <div className="border-t">{children}</div>
            ),
          }}
        />
      </AuiIf>
    </ChainOfThoughtPrimitive.Root>
  );
}
```

### 与组件的关系

[思维链指南](/docs/guides/chain-of-thought)介绍了使用 `MessagePrimitive.GroupedParts` 的端到端设置，包括使用推理模型时的后端配置。请参阅完整的[with-chain-of-thought 示例](https://github.com/assistant-ui/assistant-ui/tree/main/examples/with-chain-of-thought)

### API 参考

如需包含后端配置的完整指南，请参阅[思维链](/docs/guides/chain-of-thought)。如需了解属性详情，请参阅 [ChainOfThoughtPrimitive 源码](https://github.com/assistant-ui/assistant-ui/tree/main/packages/react/src/primitives/chainOfThought).

相关内容：
- [思维链指南](/docs/guides/chain-of-thought)
- [MessagePrimitive](/docs/primitives/message)
