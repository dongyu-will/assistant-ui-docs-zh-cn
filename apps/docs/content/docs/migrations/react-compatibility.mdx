---
title: 使用旧版 React
description: React 18 和 19 的兼容性说明。
---


<Callout type="warning" title="需要 React 18">
  assistant-ui 需要 React 18 或 React 19。不支持 React 17 和 React 16。
  如果你需要升级帮助，请加入我们的
  [Discord](https://discord.gg/S9dwgCNEFs).
</Callout>

本指南介绍如何配置 assistant-ui，使其与 React 18 配合使用。

## React 18

如果你使用 React 18，需要更新 shadcn/ui 组件，使其能够与 `forwardRef` 配合使用。具体来说，你需要修改 Button 组件。

### 更新 Button 组件

前往 Button 组件文件（通常位于 `/components/ui/button.tsx`），并使用 `forwardRef` 包裹 Button 组件：

```tsx
// Before
function Button({
  className,
  variant,
  size,
  asChild = false,
  ...props
}: React.ComponentProps<"button"> &
  VariantProps<typeof buttonVariants> & {
    asChild?: boolean;
  }) {
  const Comp = asChild ? Slot : "button";

  return (
    <Comp
      data-slot="button"
      className={cn(buttonVariants({ variant, size, className }))}
      {...props}
    />
  );
}

// After
const Button = React.forwardRef<
  HTMLButtonElement,
  React.ComponentProps<"button"> &
    VariantProps<typeof buttonVariants> & {
      asChild?: boolean;
    }
>(({ className, variant, size, asChild = false, ...props }, ref) => {
  const Comp = asChild ? Slot : "button";

  return (
    <Comp
      data-slot="button"
      className={cn(buttonVariants({ variant, size, className }))}
      ref={ref}
      {...props}
    />
  );
});
Button.displayName = "Button";
```

## 其他资源

如果你遇到 React 兼容性问题，请：

1. 检查是否已安装所有必需的依赖
2. 确保正确实现了组件修改
3. 加入我们的 [Discord](https://discord.gg/S9dwgCNEFs) 社区，获取直接支持
