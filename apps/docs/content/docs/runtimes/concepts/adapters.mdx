---
title: 适配器
description: 用于附件、语音、反馈、历史记录和建议的可复用扩展点。
---

适配器是 assistant-ui 为运行时添加文件上传或消息持久化等能力的方式，同时不会将运行时耦合到特定后端。你只需实现一个小型接口，将其插入运行时的 `adapters` 选项中，相应的 UI 界面（回形针按钮、音频按钮、历史记录重新加载）就会自动启用。

无论使用哪种运行时，本页中的每个适配器都以相同方式工作。当某个运行时支持适配器时，通过该运行时的 `adapters` 选项提供它：

```ts
const runtime = useLocalRuntime(modelAdapter, {
  adapters: { attachments, history, speech, feedback, suggestion },
});
```

框架适配器的形式相同：

```ts
const runtime = useChatRuntime({ adapters: { attachments, history } });
```

## 支持矩阵

| 适配器 | LocalRuntime | ExternalStoreRuntime | DataStream | AssistantTransport | ai-sdk | react-langgraph | react-langchain | react-google-adk | react-a2a | react-ag-ui | react-opencode |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 附件 | 是 | 是 | 是 | 是 | 是 | （通过线程状态） | （通过线程状态） | 是 | 是 | 是 | （否） |
| 语音 | 是 | 是 | 是 | （否） | 是 | 是 | 是 | 是 | 是 | 是 | （否） |
| 听写 | 是 | 是 | 是 | （否） | 是 | 是 | （否） | 是 | （否） | 是 | （否） |
| 反馈 | 是 | 是 | 是 | （否） | 是 | 是 | 是 | 是 | 是 | 是 | （否） |
| 历史记录 | 是 | （使用你的存储） | 是 | （使用线程转换器） | 是 | （通过加载） | （通过加载） | 是 | 是 | 是 | （由服务器管理） |
| 建议 | 是 | （否） | 是 | （否） | （否） | （否） | （否） | （否） | （否） | （否） | （否） |
| threadList | 是（`RemoteThreadListAdapter`） | 是（`ExternalStoreThreadListAdapter`） | 是（`RemoteThreadListAdapter`） | 是 | 是 | 是 | 是 | 是 | 是 | 是（实验性） | 内置（会话） |

`(no)` 表示该运行时目前未暴露适配器插槽。你需要下沉到更底层才能使用它。

## 附件适配器

用于处理文件和图片上传。启用后，输入框会渲染一个回形针按钮。

```ts
type AttachmentAdapter = {
  accept: string;
  add: (input: { file: File }) => Promise<PendingAttachment>;
  send: (attachment: PendingAttachment) => Promise<CompleteAttachment>;
  remove?: (attachment: Attachment) => Promise<void>;
};
```

三个生命周期方法：

- `add` 在用户选择文件时运行。上传文件，并返回状态为 `requires-action` 的记录，使输入框在发送前保存该文件。
- `send` 在用户提交消息时运行。完成上传，附加 `content` 负载，并将状态标记为 `complete`。
- `remove` 为可选方法，在用户发送前移除附件时运行。

最小上传并发送示例：

```ts
const attachmentAdapter: AttachmentAdapter = {
  accept: "image/*,application/pdf",
  async add({ file }) {
    const form = new FormData();
    form.append("file", file);
    const { id, url } = await fetch("/api/upload", {
      method: "POST",
      body: form,
    }).then((r) => r.json());
    return {
      id,
      type: file.type.startsWith("image/") ? "image" : "document",
      name: file.name,
      contentType: file.type,
      file,
      url,
      status: { type: "requires-action", reason: "composer-send" },
    };
  },
  async send(attachment) {
    return {
      ...attachment,
      status: { type: "complete" },
      content: [
        attachment.type === "image"
          ? { type: "image", image: attachment.url! }
          : { type: "text", text: `[${attachment.name}](${attachment.url})` },
      ],
    };
  },
};
```

如需支持多种文件类型，请使用 `CompositeAttachmentAdapter`：

```ts
import {
  CompositeAttachmentAdapter,
  SimpleImageAttachmentAdapter,
  SimpleTextAttachmentAdapter,
} from "@assistant-ui/react";

const attachmentAdapter = new CompositeAttachmentAdapter([
  new SimpleImageAttachmentAdapter(),
  new SimpleTextAttachmentAdapter(),
]);
```

## 语音适配器

用于助手消息的文本转语音。启用后，消息气泡会渲染一个音频按钮。

```ts
type SpeechSynthesisAdapter = {
  speak: (text: string) => Utterance;
};
```

`speak` 返回一个包含 `cancel()`、`status` 字段和 `subscribe(callback)` 的 `Utterance`。浏览器原生示例：

```ts
const speechAdapter: SpeechSynthesisAdapter = {
  speak(text) {
    const utterance = new SpeechSynthesisUtterance(text);
    const subscribers = new Set<() => void>();
    const result: SpeechSynthesisAdapter.Utterance = {
      status: { type: "running" },
      cancel: () => {
        speechSynthesis.cancel();
        result.status = { type: "ended", reason: "cancelled" };
        subscribers.forEach((cb) => cb());
      },
      subscribe(cb) {
        subscribers.add(cb);
        return () => subscribers.delete(cb);
      },
    };
    utterance.addEventListener("end", () => {
      result.status = { type: "ended", reason: "finished" };
      subscribers.forEach((cb) => cb());
    });
    speechSynthesis.speak(utterance);
    return result;
  },
};
```

## 听写适配器

用于输入框的语音转文本。启用后，输入框会渲染一个麦克风按钮。其契约与语音适配器相似。

## 反馈适配器

用于对助手消息进行点赞或点踩。启用后，消息气泡会渲染反馈按钮。

```ts
type FeedbackAdapter = {
  submit: (feedback: {
    type: "positive" | "negative";
    message: ThreadMessage;
  }) => Promise<void>;
};
```

```ts
const feedbackAdapter: FeedbackAdapter = {
  async submit({ type, message }) {
    await fetch("/api/feedback", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ messageId: message.id, rating: type }),
    });
  },
};
```

## 历史记录适配器

用于按线程持久化消息。由 `LocalRuntime` 以及基于它构建的适配器使用（`ai-sdk`、`react-google-adk`、`react-a2a`、`useDataStreamRuntime`）。

`ExternalStoreRuntime` 不会直接使用历史记录适配器，因为消息数组已经由你管理。请改为通过你的存储进行持久化。`react-langgraph` 和 `react-langchain` 从服务器端线程状态中获取持久化数据，并通过各自的 `load` 回调公开。

```ts
type ThreadHistoryAdapter = {
  load: () => Promise<{
    messages: { parentId: string | null; message: ThreadMessage }[];
  }>;
  append: (item: {
    parentId: string | null;
    message: ThreadMessage;
  }) => Promise<void>;
  resume?: (input: {
    messages: ThreadMessage[];
  }) => Promise<ReadableStream | undefined>;
  withFormat?: <Fmt>(fmt: Fmt) => ThreadHistoryAdapter;
};
```

`load` 在线程打开时运行。`append` 在每条消息完成后运行。

<Callout type="info">
`ai-sdk` 要求 `withFormat`，以便消息能够以 AI SDK `UIMessage` 对象的形式往返传递。在 AI SDK 路径中，不包含 `withFormat` 的适配器会在运行时抛出错误。完整模式请参阅 [AI SDK 历史记录文档](/docs/runtimes/ai-sdk/v7)。
</Callout>

## 建议适配器

在每条助手消息之后提出后续提示。启用后，建议选项卡会渲染在最新的助手消息下方。

```ts
type SuggestionAdapter = {
  generate: (input: {
    messages: readonly ThreadMessage[];
  }) => AsyncGenerator<{ prompt: string }[]>;
};
```

```ts
const suggestionAdapter: SuggestionAdapter = {
  async *generate({ messages }) {
    const last = messages.at(-1);
    if (!last) return;
    const response = await fetch("/api/suggestions", {
      method: "POST",
      body: JSON.stringify(last),
    });
    yield (await response.json()).suggestions;
  },
};
```

## 线程列表适配器

由于不同运行时的契约有所不同，多线程支持在单独的文档中介绍。请参阅[线程](/docs/runtimes/concepts/threads)。

## 组合适配器

适配器可以自由组合。根据需要提供任意数量的适配器；UI 界面会根据已填充的插槽启用相应功能。

```ts
const runtime = useLocalRuntime(modelAdapter, {
  adapters: {
    attachments: myAttachmentAdapter,
    history: myHistoryAdapter,
    speech: mySpeechAdapter,
    feedback: myFeedbackAdapter,
  },
});
```

## 相关内容

<Cards>
  <Card
    title="线程"
    description="多线程支持：云端、自定义数据库、ExternalStore。"
    href="/docs/runtimes/concepts/threads"
  />
  <Card
    title="架构"
    description="三层运行时模型，以及适配器如何融入其中。"
    href="/docs/runtimes/concepts/architecture"
  />
  <Card
    title="选择运行时"
    description="为你的后端选择合适的运行时。"
    href="/docs/runtimes/pick-a-runtime"
  />
</Cards>
