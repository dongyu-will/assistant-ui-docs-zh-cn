---
title: 流式传输
description: 事件处理器、消息累加器、转换、元数据和生成式 UI。
---

LangGraph 会发出丰富的事件流：顶层消息块、子图事件、自定义 UI 消息、错误和元数据。本页面介绍如何观察并响应这些事件。

## 消息累加器

`LangGraphMessageAccumulator` 允许你将从服务器接收的消息追加到客户端，以复现客户端的消息状态。

```ts
import {
  LangGraphMessageAccumulator,
  appendLangChainChunk,
} from "@assistant-ui/react-langgraph";

const accumulator = new LangGraphMessageAccumulator({
  appendMessage: appendLangChainChunk,
});

if (event.event === "messages/partial") {
  accumulator.addMessages(event.data);
}
```

## 消息转换

`convertLangChainMessages` 可将 LangChain 消息转换为 assistant-ui 的格式。在接入自定义适配器，或在运行时之外渲染消息时使用它。

```ts
import { convertLangChainMessages } from "@assistant-ui/react-langgraph";

const threadMessage = convertLangChainMessages(langChainMessage);
```

## 事件处理器

将 `eventHandlers` 传递给 `useLangGraphRuntime`，即可监听流式传输事件：

```ts
const runtime = useLangGraphRuntime({
  stream: async (messages, { initialize, ...config }) => {
    /* ... */
  },
  eventHandlers: {
    onMessageChunk: (chunk, metadata) => {
      // Fired for each chunk in messages-tuple mode.
      // metadata contains langgraph_step, langgraph_node, ls_model_name, etc.
      // For pipe-namespaced events emitted by subgraphs (e.g. messages|tools:call_abc),
      // metadata.namespace holds the suffix ("tools:call_abc"). Use it to attribute
      // a chunk to a specific subgraph.
    },
    onValues: (values) => {
      // Fired when a top-level values event is received.
      // Subgraph values events are routed to onSubgraphValues instead.
    },
    onUpdates: (updates) => {
      // Fired when a top-level updates event is received.
      // Subgraph updates events are routed to onSubgraphUpdates instead.
    },
    onSubgraphValues: (namespace, values) => {
      // Fired when a subgraph values|<namespace> event is received
      // (e.g. namespace === "tools:call_abc").
    },
    onSubgraphUpdates: (namespace, updates) => {
      // Fired when a subgraph updates|<namespace> event is received.
    },
    onMetadata: (metadata) => {
      /* thread metadata */
    },
    onInfo: (info) => {
      /* informational messages */
    },
    onError: (error) => {
      // Fired for both top-level and subgraph errors.
    },
    onSubgraphError: (namespace, error) => {
      // Additionally fired for subgraph errors with the namespace.
      // Use this to attribute a subgraph failure to its source without marking
      // the parent message as incomplete (that only happens for top-level errors).
    },
    onCustomEvent: (type, data) => {
      /* custom events */
    },
  },
});
```

## 消息元数据

使用 `streamMode: "messages-tuple"` 时，每个消息块都包含来自 LangGraph 服务器的元数据。使用 `useLangGraphMessageMetadata` 访问每条消息累积的元数据：

```ts
import { useLangGraphMessageMetadata } from "@assistant-ui/react-langgraph";

function MyComponent() {
  const metadata = useLangGraphMessageMetadata();
  // Map<string, LangGraphTupleMetadata> keyed by message ID
}
```

## 生成式 UI

LangGraph 可以通过 `push_ui_message`（Python）或 `typedUi().push()`（TypeScript），在助手消息旁发出结构化 UI 组件。assistant-ui 适配器会将这些组件转换为关联助手消息上的 [`DataMessagePart`s](/docs/tools/tool-ui)，然后使用现有的 `makeAssistantDataUI` API 进行渲染。

请参阅[生成式 UI](/docs/runtimes/langgraph/generative-ui)了解完整设置：启用 `custom` 流通道、发出 UI 消息、注册渲染器、动态加载，以及在线程切换时持久化 UI 状态。

## 运行期间将消息加入队列

设置 `unstable_enableMessageQueue`，即可在运行进行流式传输时继续使用输入框。排队运行期间发送的消息默认会执行 steer——它会进入 steer 通道，位于之前已排队的项目之前；而 `send({ steer: false })` 会将后续消息排在这些项目之后。待处理项目会保存在 `composer.queue` 中，并在运行结束后依次处理。

```tsx
const runtime = useLangGraphRuntime({
  stream,
  unstable_enableMessageQueue: true,
});
```

使用 [`ComposerPrimitive.Queue`](/docs/api-reference/primitives/composer) 和 [`QueueItemPrimitive`](/docs/api-reference/primitives/queue-item) 渲染待处理消息。

## 每次运行的配置

发送消息时，其 `runConfig` 会转发给 `stream`；如果使用 `unstable_createLangGraphStream`，还会作为 LangGraph SDK 运行的 `config` 发布。前端自动恢复工具结果以及 `useLangGraphSendCommand` 会复用生成待处理工具调用或中断的运行的 `runConfig`。在 `useLangGraphSend` 上显式设置的 `runConfig` 仍具有优先级。重新获取线程时，如果仍携带该中断，则会保留其所有者。未在本地记录所有权的历史记录在恢复时保持未配置状态；该记录仅限当前会话，在重新加载后不会保留。

## 下一步

<Cards>
  <Card
    title="生成式 UI"
    description="由你的图发出的结构化 UI 组件。"
    href="/docs/runtimes/langgraph/generative-ui"
  />
  <Card
    title="中断"
    description="中断持久化和基于检查点的消息编辑。"
    href="/docs/runtimes/langgraph/interrupts"
  />
  <Card
    title="线程"
    description="基本线程支持、AssistantCloud、自定义线程列表适配器。"
    href="/docs/runtimes/langgraph/threads"
  />
</Cards>
