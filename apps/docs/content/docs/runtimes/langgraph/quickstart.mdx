---
title: 快速开始
description: 通过模板和手动配置路径，构建可运行的 LangGraph 聊天。
---

通过两种方式，连接 LangGraph Cloud 服务器并运行聊天。模板方式最快；当你需要集成到现有项目时，可以参考并调整手动配置方式。

## 从模板开始

<PlatformTabs>
<Tab value="React">

```sh
npx create-assistant-ui@latest -t langchain my-app
cd my-app
```

设置环境变量：

```sh title=".env.local"
# LANGCHAIN_API_KEY=your_api_key       # production
# LANGGRAPH_API_URL=your_api_url       # production
NEXT_PUBLIC_LANGGRAPH_API_URL=your_api_url           # development (no API key required)
NEXT_PUBLIC_LANGGRAPH_ASSISTANT_ID=your_graph_id
```

```sh
npm run dev
```

跳转到[流式传输](/docs/runtimes/langgraph/streaming)部分，开始添加功能。

</Tab>
<Tab value="React Native">

目前还没有 React Native LangGraph 模板。请在 Expo 应用中使用下面的手动配置方式。

</Tab>
<Tab value="React Ink">

目前还没有 Ink LangGraph 模板。请在 Node/Ink 应用中使用下面的手动配置方式。

</Tab>
</PlatformTabs>

## 在现有项目中手动配置

<Steps>
<Step>

### 安装依赖

<PlatformTabs>
<Tab value="React">

<InstallCommand npm={["@assistant-ui/react", "@assistant-ui/react-langgraph", "@langchain/langgraph-sdk"]} />

</Tab>
<Tab value="React Native">

<InstallCommand expo={["@assistant-ui/react-native", "@assistant-ui/react-langgraph", "@langchain/langgraph-sdk"]} />

</Tab>
<Tab value="React Ink">

<InstallCommand npm={["@assistant-ui/react-ink", "@assistant-ui/react-langgraph", "@langchain/langgraph-sdk", "ink", "react"]} />

</Tab>
</PlatformTabs>

</Step>
<Step>

### 创建 LangGraph 客户端辅助工具

<PlatformTabs>
<Tab value="React">

```ts title="@/lib/chatApi.ts"
import { Client } from "@langchain/langgraph-sdk";

export const createClient = () => {
  const apiUrl =
    process.env["NEXT_PUBLIC_LANGGRAPH_API_URL"] ||
    (typeof window !== "undefined"
      ? new URL("/api", window.location.href).href
      : "/api");
  return new Client({ apiUrl });
};
```

</Tab>
<Tab value="React Native">

```ts title="lib/chatApi.ts"
import { Client } from "@langchain/langgraph-sdk";

export const createClient = () => {
  const apiUrl =
    process.env.EXPO_PUBLIC_LANGGRAPH_API_URL ?? "http://localhost:2024";
  return new Client({ apiUrl });
};
```

</Tab>
<Tab value="React Ink">

```ts title="lib/chatApi.ts"
import { Client } from "@langchain/langgraph-sdk";

export const createClient = () => {
  const apiUrl = process.env.LANGGRAPH_API_URL ?? "http://localhost:2024";
  return new Client({ apiUrl });
};
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 构建助手组件

<PlatformTabs>
<Tab value="React">

```tsx title="@/components/MyAssistant.tsx"
"use client";

import { useMemo } from "react";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";
import { AssistantRuntimeProvider } from "@assistant-ui/react";
import {
  unstable_createLangGraphStream,
  useLangGraphRuntime,
  type LangChainMessage,
} from "@assistant-ui/react-langgraph";

import { createClient } from "@/lib/chatApi";

const ASSISTANT_ID = process.env["NEXT_PUBLIC_LANGGRAPH_ASSISTANT_ID"]!;

export function MyAssistant() {
  const client = useMemo(() => createClient(), []);
  const stream = useMemo(
    () =>
      unstable_createLangGraphStream({
        client,
        assistantId: ASSISTANT_ID,
      }),
    [client],
  );

  const runtime = useLangGraphRuntime({
    unstable_allowCancellation: true,
    stream,
    create: async () => {
      const { thread_id } = await client.threads.create();
      return { externalId: thread_id };
    },
    load: async (externalId) => {
      const state = await client.threads.getState<{
        messages: LangChainMessage[];
      }>(externalId);
      return {
        messages: state.values.messages,
        interrupts: state.tasks[0]?.interrupts,
      };
    },
  });

  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Native">

```tsx title="components/MyAssistant.tsx"
import { useMemo } from "react";
import { AssistantRuntimeProvider } from "@assistant-ui/react-native";
import {
  unstable_createLangGraphStream,
  useLangGraphRuntime,
  type LangChainMessage,
} from "@assistant-ui/react-langgraph";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";

import { createClient } from "@/lib/chatApi";

const ASSISTANT_ID = process.env.EXPO_PUBLIC_LANGGRAPH_ASSISTANT_ID!;

export function MyAssistant() {
  const client = useMemo(() => createClient(), []);
  const stream = useMemo(
    () =>
      unstable_createLangGraphStream({
        client,
        assistantId: ASSISTANT_ID,
      }),
    [client],
  );

  const runtime = useLangGraphRuntime({
    unstable_allowCancellation: true,
    stream,
    create: async () => {
      const { thread_id } = await client.threads.create();
      return { externalId: thread_id };
    },
    load: async (externalId) => {
      const state = await client.threads.getState<{
        messages: LangChainMessage[];
      }>(externalId);
      return {
        messages: state.values.messages,
        interrupts: state.tasks[0]?.interrupts,
      };
    },
  });

  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Ink">

```tsx title="components/MyAssistant.tsx"
import { useMemo } from "react";
import { AssistantRuntimeProvider } from "@assistant-ui/react-ink";
import {
  unstable_createLangGraphStream,
  useLangGraphRuntime,
  type LangChainMessage,
} from "@assistant-ui/react-langgraph";
import { Thread } from "./thread.js";

import { createClient } from "../lib/chatApi.js";

const ASSISTANT_ID = process.env.LANGGRAPH_ASSISTANT_ID!;

export function MyAssistant() {
  const client = useMemo(() => createClient(), []);
  const stream = useMemo(
    () =>
      unstable_createLangGraphStream({
        client,
        assistantId: ASSISTANT_ID,
      }),
    [client],
  );

  const runtime = useLangGraphRuntime({
    unstable_allowCancellation: true,
    stream,
    create: async () => {
      const { thread_id } = await client.threads.create();
      return { externalId: thread_id };
    },
    load: async (externalId) => {
      const state = await client.threads.getState<{
        messages: LangChainMessage[];
      }>(externalId);
      return {
        messages: state.values.messages,
        interrupts: state.tasks[0]?.interrupts,
      };
    },
  });

  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 挂载组件

<PlatformTabs>
<Tab value="React">

```tsx title="@/app/page.tsx"
import { MyAssistant } from "@/components/MyAssistant";

export default function Home() {
  return (
    <main className="h-dvh">
      <MyAssistant />
    </main>
  );
}
```

</Tab>
<Tab value="React Native">

```tsx title="app/index.tsx"
import { View } from "react-native";
import { MyAssistant } from "@/components/MyAssistant";

export default function Home() {
  return (
    <View style={{ flex: 1 }}>
      <MyAssistant />
    </View>
  );
}
```

</Tab>
<Tab value="React Ink">

```tsx title="app.tsx"
import { Box } from "ink";
import { MyAssistant } from "./components/MyAssistant.js";

export function App() {
  return (
    <Box flexDirection="column">
      <MyAssistant />
    </Box>
  );
}
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 设置环境变量

<PlatformTabs>
<Tab value="React">

使用与上方模板路径相同的 `.env.local` 结构，参见[上文](#from-the-template)。

</Tab>
<Tab value="React Native">

```sh title=".env"
EXPO_PUBLIC_LANGGRAPH_API_URL=http://localhost:2024
EXPO_PUBLIC_LANGGRAPH_ASSISTANT_ID=your_graph_id
```

</Tab>
<Tab value="React Ink">

```sh title=".env"
LANGGRAPH_API_URL=http://localhost:2024
LANGGRAPH_ASSISTANT_ID=your_graph_id
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 配置 UI 组件

<PlatformTabs>
<Tab value="React">

请参阅 [UI 组件指南](/elements/thread)，为 Thread、输入框和支持性原语完成连接配置。

</Tab>
<Tab value="React Native">

请按照 [React Native 配置指南](/docs/react-native)，添加原生 Thread、输入框和支持性原语。

</Tab>
<Tab value="React Ink">

请按照 [Ink 配置指南](/docs/ink)，添加终端 Thread、输入框和支持性原语。

</Tab>
</PlatformTabs>

</Step>
</Steps>

## 生产环境代理后端

在开发环境中，上面的客户端使用 `NEXT_PUBLIC_LANGGRAPH_API_URL` 直接访问 LangGraph Cloud。在生产环境中，请通过你自己的后端进行代理，确保 API 密钥不会传递到客户端。请将代理限制为实际需要的端点。

```ts title="@/app/api/[...path]/route.ts"
import { NextRequest, NextResponse } from "next/server";

export const runtime = "edge";

function getCorsHeaders() {
  return {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, POST, PUT, PATCH, DELETE, OPTIONS",
    "Access-Control-Allow-Headers": "*",
  };
}

async function handleRequest(req: NextRequest, method: string) {
  try {
    const path = req.nextUrl.pathname.replace(/^\/?api\//, "");
    const url = new URL(req.url);
    const searchParams = new URLSearchParams(url.search);
    searchParams.delete("_path");
    searchParams.delete("nxtP_path");
    const queryString = searchParams.toString()
      ? `?${searchParams.toString()}`
      : "";

    const options: RequestInit = {
      method,
      headers: { "x-api-key": process.env["LANGCHAIN_API_KEY"] ?? "" },
      signal: req.signal,
    };
    if (["POST", "PUT", "PATCH"].includes(method)) {
      options.body = await req.text();
    }

    const res = await fetch(
      `${process.env["LANGGRAPH_API_URL"]}/${path}${queryString}`,
      options,
    );
    const headers = new Headers(res.headers);
    headers.delete("content-encoding");
    headers.delete("content-length");
    headers.delete("transfer-encoding");
    for (const [key, value] of Object.entries(getCorsHeaders())) {
      headers.set(key, value);
    }
    return new NextResponse(res.body, {
      status: res.status,
      statusText: res.statusText,
      headers,
    });
  } catch (e: unknown) {
    if (e instanceof Error) {
      const typedError = e as Error & { status?: number };
      return NextResponse.json(
        { error: typedError.message },
        { status: typedError.status ?? 500 },
      );
    }
    return NextResponse.json({ error: "Unknown error" }, { status: 500 });
  }
}

export const GET = (req: NextRequest) => handleRequest(req, "GET");
export const POST = (req: NextRequest) => handleRequest(req, "POST");
export const PUT = (req: NextRequest) => handleRequest(req, "PUT");
export const PATCH = (req: NextRequest) => handleRequest(req, "PATCH");
export const DELETE = (req: NextRequest) => handleRequest(req, "DELETE");
export const OPTIONS = () =>
  new NextResponse(null, { status: 204, headers: getCorsHeaders() });
```

配置此路由后，从生产环境变量中移除 `NEXT_PUBLIC_LANGGRAPH_API_URL`；客户端辅助工具会回退到同源的 `/api` 路径。请改为在服务器端设置 `LANGCHAIN_API_KEY` 和 `LANGGRAPH_API_URL`。

## 下一步

<Cards>
  <Card
    title="流式传输"
    description="事件处理器、消息元数据和消息转换。"
    href="/docs/runtimes/langgraph/streaming"
  />
  <Card
    title="生成式 UI"
    description="由你的图生成的结构化 UI 组件。"
    href="/docs/runtimes/langgraph/generative-ui"
  />
  <Card
    title="中断"
    description="中断持久化，以及基于检查点的消息编辑。"
    href="/docs/runtimes/langgraph/interrupts"
  />
</Cards>
