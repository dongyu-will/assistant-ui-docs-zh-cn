---
title: 线程
description: 基本线程支持、AssistantCloud 和自定义线程列表适配器。
---

`useLangGraphRuntime` 支持 [threads](/docs/runtimes/concepts/threads) 中所记录的同一三路径线程模型，并针对 LangGraph 线程 ID 进行了调整。本页面介绍 LangGraph 特有的连接配置。

## 基本线程支持

`useLangGraphRuntime` 内置了线程管理功能：

```ts
const runtime = useLangGraphRuntime({
  stream: async (messages, { initialize, ...config }) => {
    // initialize() creates or loads a thread and returns its IDs
    const { remoteId, externalId } = await initialize();
    // Use externalId (your backend's thread ID) for API calls
    return sendMessage({ threadId: externalId, messages, config });
  },
  create: async () => {
    // Called when creating a new thread
    const { thread_id } = await createThread();
    return { externalId: thread_id };
  },
  load: async (externalId) => {
    // Called when loading an existing thread
    const state = await getThreadState(externalId);
    return {
      messages: state.values.messages,
      interrupts: state.tasks[0]?.interrupts,
    };
  },
});
```

## 云端持久化

如需托管多线程支持、持久化、同步和标题，请传入一个 `AssistantCloud` 实例：

```ts
const runtime = useLangGraphRuntime({
  cloud, // see "AssistantCloud" in /docs/runtimes/concepts/threads
  // ... stream, create, load functions
});
```

有关设置详情，请参阅[云端持久化指南](/docs/cloud/langgraph)。

## 自定义线程列表

如果不运行 assistant-cloud，但希望在​​线程选择器中显示已有的 LangGraph `thread_id`，请通过 `unstable_threadListAdapter` 传入一个 `RemoteThreadListAdapter`。一种常见的实现方式是使用 `client.threads.search()` 支持 `list()`，并使用 `client.threads.create()` 支持 `initialize()`。

```ts
import type { RemoteThreadListAdapter } from "@assistant-ui/react";
import { Client } from "@langchain/langgraph-sdk";

const client = new Client({
  apiUrl: process.env.NEXT_PUBLIC_LANGGRAPH_API_URL,
});

const threadListAdapter: RemoteThreadListAdapter = {
  async list() {
    const threads = await client.threads.search({ limit: 50 });
    return {
      threads: threads.map((t) => ({
        status: "regular",
        remoteId: t.thread_id,
        externalId: t.thread_id,
        title: (t.metadata as { title?: string } | undefined)?.title,
      })),
    };
  },
  async initialize() {
    const t = await client.threads.create();
    return { remoteId: t.thread_id, externalId: t.thread_id };
  },
  async delete(remoteId) {
    await client.threads.delete(remoteId);
  },
  // rename, archive, unarchive, fetch, generateTitle — see the threads concept page
};

const runtime = useLangGraphRuntime({
  stream: async function* (messages, { initialize }) {
    /* ... */
  },
  load: async (externalId) => {
    /* ... */
  },
  unstable_threadListAdapter: threadListAdapter,
});
```

设置 `remoteId === externalId` 可使 assistant-ui 存储的 ID 与 LangGraph 线程 ID 保持一致，而你的 `load` 和 `stream` 回调会接收这些线程 ID。有关完整的适配器约定，请参阅 [threads](/docs/runtimes/concepts/threads)。

<Callout type="info">
提供 `unstable_threadListAdapter` 后，`cloud`、`create` 和 `delete` 选项将被忽略；适配器负责完整的线程列表生命周期。
</Callout>

## 下一步

<Cards>
  <Card
    title="流式传输"
    description="事件处理器、消息元数据、生成式 UI。"
    href="/docs/runtimes/langgraph/streaming"
  />
  <Card
    title="线程（概念）"
    description="跨运行时的通用多线程模型。"
    href="/docs/runtimes/concepts/threads"
  />
</Cards>
