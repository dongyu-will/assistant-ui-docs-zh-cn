---
title: AI SDK v6（旧版）
description: 面向仍在使用 AI SDK v6 的项目的参考文档。新项目应使用 v7。
---

<Callout type="warn">
AI SDK v6 是旧版。新项目应使用 [AI SDK v7](/docs/runtimes/ai-sdk/v7)。v6 用户应固定使用 `@assistant-ui/react-ai-sdk@1.3.40`（最后一个兼容 v6 的版本）；后续版本面向 v7。
</Callout>

需要 `ai@^6` 和 `@ai-sdk/react@^3`。其他版本请参阅[概览](/docs/runtimes/ai-sdk/overview)。

## 快速开始

<Steps>
<Step>

### 创建项目

<PlatformTabs>
<Tab value="React">

```sh
npx create-next-app@latest my-app
cd my-app
```

</Tab>
<Tab value="React Native">

```sh
npx create-expo-app@latest my-app
cd my-app
```

</Tab>
<Tab value="React Ink">

```sh
mkdir my-app
cd my-app
npm init -y
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 安装依赖

<PlatformTabs>
<Tab value="React">

<InstallCommand npm={["@assistant-ui/react", "@assistant-ui/react-ai-sdk@1.3.40", "ai@^6", "@ai-sdk/react@^3", "@ai-sdk/openai", "zod"]} />

</Tab>
<Tab value="React Native">

<InstallCommand expo={["@assistant-ui/react-native", "@assistant-ui/react-ai-sdk@1.3.40", "ai@^6", "@ai-sdk/react@^3", "@ai-sdk/openai", "zod"]} />

</Tab>
<Tab value="React Ink">

<InstallCommand npm={["@assistant-ui/react-ink", "@assistant-ui/react-ai-sdk@1.3.40", "ai@^6", "@ai-sdk/react@^3", "@ai-sdk/openai", "zod", "ink", "react"]} />

</Tab>
</PlatformTabs>

</Step>
<Step>

### 设置后端路由

对于 React Native 和 Ink，请在单独的后端项目中托管此路由，并将客户端运行时指向其绝对 URL。

```ts title="@/app/api/chat/route.ts"
import { openai } from "@ai-sdk/openai";
import {
  streamText,
  convertToModelMessages,
  tool,
  zodSchema,
} from "ai";
import type { UIMessage } from "ai";
import { z } from "zod";

export const maxDuration = 30;

export async function POST(req: Request) {
  const { messages }: { messages: UIMessage[] } = await req.json();

  const result = streamText({
    model: openai("gpt-5.6-luna"),
    messages: await convertToModelMessages(messages), // async in v6
    tools: {
      get_current_weather: tool({
        description: "Get the current weather",
        inputSchema: zodSchema(z.object({ city: z.string() })),
        execute: async ({ city }) => {
          return `The weather in ${city} is sunny`;
        },
      }),
    },
  });

  return result.toUIMessageStreamResponse();
}
```

</Step>
<Step>

### 设置前端

<PlatformTabs>
<Tab value="React">

```tsx title="@/app/page.tsx"
"use client";

import { Thread } from "@/components/assistant-ui/elements/thread.aui";
import { AssistantRuntimeProvider } from "@assistant-ui/react";
import { useChatRuntime } from "@assistant-ui/react-ai-sdk";

export default function Home() {
  const runtime = useChatRuntime();
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <div className="h-full">
        <Thread />
      </div>
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Native">

```tsx title="app/index.tsx"
import { AssistantRuntimeProvider } from "@assistant-ui/react-native";
import {
  AssistantChatTransport,
  useChatRuntime,
} from "@assistant-ui/react-ai-sdk";
import { View } from "react-native";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";

const API_URL = process.env.EXPO_PUBLIC_API_URL ?? "http://localhost:3000";

export default function Home() {
  const runtime = useChatRuntime({
    transport: new AssistantChatTransport({
      api: `${API_URL}/api/chat`,
    }),
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <View style={{ flex: 1 }}>
        <Thread />
      </View>
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Ink">

```tsx title="app.tsx"
import { AssistantRuntimeProvider } from "@assistant-ui/react-ink";
import {
  AssistantChatTransport,
  useChatRuntime,
} from "@assistant-ui/react-ai-sdk";
import { Box } from "ink";
import { Thread } from "./components/thread.js";

export function App() {
  const runtime = useChatRuntime({
    transport: new AssistantChatTransport({
      api: "http://localhost:3000/api/chat",
    }),
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Box flexDirection="column">
        <Thread />
      </Box>
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 设置 UI 组件

<PlatformTabs>
<Tab value="React">

按照 [UI 组件指南](/elements/thread) 配置 Thread、输入框和支持性的原语。

</Tab>
<Tab value="React Native">

按照 [React Native 设置指南](/docs/react-native) 添加原生 Thread、输入框和支持性的原语。

</Tab>
<Tab value="React Ink">

按照 [Ink 设置指南](/docs/ink) 添加终端 Thread、输入框和支持性的原语。

</Tab>
</PlatformTabs>

</Step>
</Steps>

## 前端工具和系统消息

`AssistantChatTransport`（默认使用）会在每次请求中将系统消息和前端工具转发到后端。使用 `frontendTools` 消费它们：

```ts title="@/app/api/chat/route.ts"
import { openai } from "@ai-sdk/openai";
import { streamText, convertToModelMessages, zodSchema } from "ai";
import type { UIMessage } from "ai";
import { frontendTools } from "@assistant-ui/react-ai-sdk";

export async function POST(req: Request) {
  const {
    messages,
    system,
    tools,
  }: {
    messages: UIMessage[];
    system?: string;
    tools?: any;
  } = await req.json();

  const result = streamText({
    model: openai("gpt-5.6-luna"),
    system,
    messages: await convertToModelMessages(messages),
    tools: {
      ...frontendTools(tools),
      // your backend tools...
    },
  });

  return result.toUIMessageStreamResponse();
}
```

前端工具通过 provider 的 `config` 注册（参见[工具指南](/docs/tools/defining-tools)），并通过 `frontendTools` 序列化后发送到后端。

## 多步工具调用

AI SDK v6 支持通过 `stopWhen` 在单个请求中运行多轮工具调用。使用 `stepCountIs(N)` 限制迭代次数：

```ts title="@/app/api/chat/route.ts"
import { openai } from "@ai-sdk/openai";
import {
  streamText,
  convertToModelMessages,
  stepCountIs,
} from "ai";

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = streamText({
    model: openai("gpt-5.6-luna"),
    messages: await convertToModelMessages(messages),
    tools: {
      /* ... */
    },
    stopWhen: stepCountIs(10), // cap at 10 tool-call iterations
  });

  return result.toUIMessageStreamResponse();
}
```

如果没有 `stopWhen`，AI SDK 只会运行一个推理步骤。当工具链需要进行多次调用时，请设置 `stepCountIs`（或 AI SDK 的其他停止条件之一）。

## 服务端工具审批

AI SDK v6 允许工具通过 `needsApproval` 控制自身的执行。服务器会暂停并发出一个 `approval-requested` part，客户端提交审批响应后再恢复。assistant-ui 会在工具 part 上将此控制显示为 `approval`，并在渲染器上添加 `respondToApproval` prop，使工具组件与 `chatHelpers` 保持解耦。

在后端，使用 `needsApproval` 标记工具（布尔值或函数）：

```ts title="@/app/api/chat/route.ts"
import { openai } from "@ai-sdk/openai";
import { streamText, convertToModelMessages, tool } from "ai";
import { z } from "zod";

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = streamText({
    model: openai("gpt-5.6-luna"),
    messages: await convertToModelMessages(messages),
    tools: {
      deploy: tool({
        description: "Deploy the current build to an environment.",
        inputSchema: z.object({ target: z.string() }),
        needsApproval: ({ input }) => input.target === "production",
        execute: async ({ target }) => ({ deployed: target }),
      }),
    },
  });

  return result.toUIMessageStreamResponse();
}
```

在客户端，使用 `sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithApprovalResponses` 配置 `useChat`，这样用户做出决定后，响应就会发送回服务器：

```tsx title="@/app/page.tsx"
"use client";

import { useChat } from "@ai-sdk/react";
import {
  AssistantRuntimeProvider,
  useAISDKRuntime,
} from "@assistant-ui/react-ai-sdk";
import { lastAssistantMessageIsCompleteWithApprovalResponses } from "ai";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";

export default function Page() {
  const chat = useChat({
    sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithApprovalResponses,
  });
  const runtime = useAISDKRuntime(chat);

  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

使用工具包条目渲染此控制。`approval` 字段携带控制状态，而 `respondToApproval` 是确认它的唯一正确方式（它会从 part 中读取审批 ID）：

```tsx title="@/app/page.tsx"
"use client";

import { useChat } from "@ai-sdk/react";
import {
  AssistantRuntimeProvider,
  AuiConfig,
  defineToolkit,
  Tools,
  type ToolApprovalResponse,
} from "@assistant-ui/react";
import { useAISDKRuntime } from "@assistant-ui/react-ai-sdk";
import { lastAssistantMessageIsCompleteWithApprovalResponses } from "ai";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";

const toolkit = defineToolkit({
  deploy: {
    type: "backend",
    render: ({ args, approval, respondToApproval, result }) => {
      // A refused response rejects and a precondition throws, so `try`/`await`
      // covers both and the controls stay actionable.
      const answer = async (response: ToolApprovalResponse) => {
        try {
          await respondToApproval(response);
        } catch (failure) {
          console.error(failure);
        }
      };

      if (approval?.approved === undefined) {
        return (
          <div>
            <p>Approve deploy to {args.target}?</p>
            <button onClick={() => void answer({ approved: true })}>
              Approve
            </button>
            <button
              onClick={() =>
                void answer({ approved: false, reason: "user denied" })
              }
            >
              Deny
            </button>
          </div>
        );
      }
      if (approval?.approved === false) return <p>Denied</p>;
      if (result === undefined) return <p>Approved, deploying…</p>;
      return <p>Deployed {result.deployed}</p>;
    },
  },
});

export default function Page() {
  const chat = useChat({
    sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithApprovalResponses,
  });
  const runtime = useAISDKRuntime(chat);
  const config = AuiConfig({ tools: Tools({ toolkit }) });
  return (
    <AssistantRuntimeProvider runtime={runtime} config={config}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

请参阅[工具 UI 指南](/docs/tools/tool-ui#server-side-approval-gates)，了解 `approval.approved` 的完整三状态语义以及 `isAutomatic` 标志。

## 引用上下文

assistant-ui 的输入框可以将引用元数据附加到用户消息上（例如，用户选择文本并点击“引用”时）。在服务器端，`injectQuoteContext` 会将这些元数据展平为 Markdown 引用块前缀，使 LLM 能够看到引用的文本：

```ts title="@/app/api/chat/route.ts"
import {
  streamText,
  convertToModelMessages,
} from "ai";
import { injectQuoteContext } from "@assistant-ui/react-ai-sdk";
import { openai } from "@ai-sdk/openai";

export async function POST(req: Request) {
  const { messages } = await req.json();
  const result = streamText({
    model: openai("gpt-5.6-luna"),
    messages: await convertToModelMessages(injectQuoteContext(messages)),
  });
  return result.toUIMessageStreamResponse();
}
```

`injectQuoteContext` 具有幂等性：如果已经存在引用块前缀，则不会重复添加。

## Token 使用量

`useThreadTokenUsage` 会在客户端公开线程级别的 token 使用量。在服务器端，通过 `messageMetadata` 附加 `usage` 和 `modelId`：

```ts title="@/app/api/chat/route.ts"
import { streamText, convertToModelMessages } from "ai";
import { frontendTools } from "@assistant-ui/react-ai-sdk";

export async function POST(req: Request) {
  const { messages, tools, config } = await req.json();
  const result = streamText({
    model: getModel(config?.modelName),
    messages: await convertToModelMessages(messages),
    tools: frontendTools(tools),
  });
  return result.toUIMessageStreamResponse({
    messageMetadata: ({ part }) => {
      if (part.type === "finish") {
        return { usage: part.totalUsage };
      }
      if (part.type === "finish-step") {
        return { modelId: part.response.modelId };
      }
      return undefined;
    },
  });
}
```

```tsx title="@/components/TokenCounter.tsx"
"use client";

import { useThreadTokenUsage } from "@assistant-ui/react-ai-sdk";

export function TokenCounter() {
  const usage = useThreadTokenUsage();
  if (!usage) return null;
  return <div>{usage.totalTokens} total tokens</div>;
}
```

如果需要以内联方式显示每条消息的使用量，`getThreadMessageTokenUsage(message)` 会返回按消息划分的使用量。

## 附件

附件通过标准的[附件适配器](/docs/runtimes/concepts/adapters#attachment-adapter)工作。通过 `useChatRuntime` 传入：

```tsx
const runtime = useChatRuntime({
  adapters: { attachments: myAttachmentAdapter },
});
```

附件适配器的 `send` 应返回符合 AI SDK 形状的 `content` parts，例如文件：

```ts
return {
  ...attachment,
  status: { type: "complete" },
  content: [
    {
      type: "file",
      mimeType: attachment.contentType ?? "",
      filename: attachment.name,
      data: await getFileDataURL(attachment.file),
    },
  ],
};
```

对于图像，请使用 `{ type: "image", image: <data url or remote url> }`。AI SDK 会将这些内容传递给接受多模态内容的 provider。

## 持久化聊天历史

默认情况下，消息只存在于内存中，重新加载页面后会重置。若要按线程持久化并恢复历史记录，请通过 `adapters.history` 提供 `ThreadHistoryAdapter`。

<Callout type="warn">
适配器**必须**实现 `withFormat`。`useChatRuntime` 会通过 `withFormat(fmt)` 持久化历史记录，使消息能够以 AI SDK `UIMessage` 对象的形式往返传递。缺少 `withFormat` 的适配器会在运行时抛出异常；AI SDK 路径不会使用顶层的 `load` 和 `append`。
</Callout>

<Callout type="info">
如需无需编写适配器代码的服务端云端持久化，请参阅 [AssistantCloud 集成](/docs/cloud/ai-sdk-assistant-ui)。
</Callout>

```tsx
"use client";

import { useChatRuntime } from "@assistant-ui/react-ai-sdk";
import type { ThreadHistoryAdapter } from "@assistant-ui/react";

const historyAdapter: ThreadHistoryAdapter = {
  // Required by the type — unused by useChatRuntime.
  async load() {
    return { headId: null, messages: [] };
  },
  async append() {},

  // fmt encodes UIMessage <-> storage rows (ai-sdk/v6 format).
  withFormat: (fmt) => ({
    async load() {
      const rows = await fetch("/api/history").then((r) => r.json());
      return { messages: rows.map(fmt.decode) };
    },
    async append(item) {
      await fetch("/api/history", {
        method: "POST",
        body: JSON.stringify({
          id: fmt.getId(item.message),
          parent_id: item.parentId,
          format: fmt.format,
          content: fmt.encode(item),
        }),
      });
    },
  }),
};

function Chat() {
  const runtime = useChatRuntime({ adapters: { history: historyAdapter } });
  // ...
}
```

每条持久化记录都遵循 `{ id, parent_id, format, content }`。`fmt.encode` 会生成 `content` payload，而 `fmt.decode` 会将其还原，因此后端无需了解 `UIMessage` 的内部实现。

## 自定义传输层

`AssistantChatTransport` 是默认实现。若要指向非默认端点，请使用自定义 `api` 实例化它：

```tsx
import {
  useChatRuntime,
  AssistantChatTransport,
} from "@assistant-ui/react-ai-sdk";

const runtime = useChatRuntime({
  transport: new AssistantChatTransport({ api: "/my-custom-api/chat" }),
});
```

如果需要的传输层不继承自 `AssistantChatTransport`，就无法获得系统消息和前端工具的自动转发；此时该传输层只是普通的 AI SDK `ChatTransport`，所有内容都需要由你显式控制。

## 高级用法：useAISDKRuntime

当需要直接访问 `useChat` 实例时（例如要与非 assistant-ui 代码共享它），可以降级使用 `useAISDKRuntime`：

```tsx
import { useChat } from "@ai-sdk/react";
import { useAISDKRuntime } from "@assistant-ui/react-ai-sdk";

const chat = useChat();
const runtime = useAISDKRuntime(chat);
```

`useAISDKRuntime` 不提供 `cloud` 或更高层级的适配器插槽；这些属于 `useChatRuntime`。如果既需要访问自己的 `useChat`，又需要云端线程支持，通常应使用 `useChatRuntime`，并改用 assistant-ui hooks 读取聊天状态。

## 适配器支持

| 适配器 | 支持方式 |
| --- | --- |
| 附件 | `adapters.attachments` |
| 语音 | `adapters.speech` |
| 听写 | `adapters.dictation` |
| 反馈 | `adapters.feedback` |
| 历史记录 | `adapters.history`（必须实现 `withFormat`） |
| threadList | 通过 `cloud`（托管）或 [RemoteThreadListRuntime](/docs/runtimes/concepts/threads)（自定义数据库） |

## v5 的主要变化

| 功能 | v5 | v6 |
| --- | --- | --- |
| `ai` 包 | `ai@^5` | `ai@^6` |
| `@ai-sdk/react` | `@ai-sdk/react@^2` | `@ai-sdk/react@^3` |
| `convertToModelMessages` | 同步 | 异步（`await`） |
| 工具 schema | `parameters: z.object({...})` | `inputSchema: zodSchema(z.object({...}))` |
| 响应 | `toDataStreamResponse()` | `toUIMessageStreamResponse()` |

## 示例

维护中的示例面向 v7：[`examples/with-ai-sdk-v7`](https://github.com/assistant-ui/assistant-ui/tree/main/examples/with-ai-sdk-v7). 该示例的最后一个 v6 修订版位于仓库历史中，与 `@assistant-ui/react-ai-sdk` 1.x 版本一起保存。

对于需要在流式传输过程中重新加载页面后仍能保留的响应，请参阅[可恢复流](/docs/guides/resumable-streams)，它会将相同的 v6 字节流封装在 `assistant-stream/resumable` 中。

## 下一步

<Cards>
  <Card
    title="适配器"
    description="附件、语音、反馈、历史记录、建议；单一事实来源。"
    href="/docs/runtimes/concepts/adapters"
  />
  <Card
    title="线程"
    description="单线程、AssistantCloud、自定义线程列表。"
    href="/docs/runtimes/concepts/threads"
  />
  <Card
    title="可恢复流"
    description="在响应过程中应对页面重新加载和连接断开。"
    href="/docs/guides/resumable-streams"
  />
</Cards>
