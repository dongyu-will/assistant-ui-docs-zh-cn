---
title: 快速入门
description: 使用 createAdkApiRoute 完成最小 API 路由和客户端设置。
---

只需四步即可运行 ADK 聊天。假设你已经安装了软件包，并已在服务器端准备好 ADK；如果还没有，请从[概览](/docs/runtimes/google-adk/overview)开始。

<Steps>
<Step>

### 创建后端 API 端点

使用 `createAdkApiRoute`，只需一行代码即可创建 API 路由：
对于 React Native 和 Ink，请在单独的后端项目中托管此路由，并将客户端运行时指向其绝对 URL。

```ts title="app/api/chat/route.ts"
import { createAdkApiRoute } from "@assistant-ui/react-google-adk/server";
import { InMemoryRunner, LlmAgent } from "@google/adk";

const agent = new LlmAgent({
  name: "my_agent",
  model: "gemini-2.5-flash",
  instruction: "You are a helpful assistant.",
});

const runner = new InMemoryRunner({ agent, appName: "my-app" });

export const POST = createAdkApiRoute({
  runner,
  userId: "user_1",
  sessionId: (req) =>
    new URL(req.url).searchParams.get("sessionId") ?? "default",
});
```

`userId` 和 `sessionId` 都可以接受静态字符串或 `(req: Request) => string`，以便从 Cookie、请求头或查询参数中动态解析。

</Step>
<Step>

### 设置客户端运行时

使用 `createAdkStream` 连接到你的 API 路由。无需手动解析 SSE：

<PlatformTabs>
<Tab value="React">

```tsx title="components/MyAssistant.tsx"
"use client";

import { AssistantRuntimeProvider } from "@assistant-ui/react";
import {
  useAdkRuntime,
  createAdkStream,
} from "@assistant-ui/react-google-adk";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";

export function MyAssistant() {
  const runtime = useAdkRuntime({
    stream: createAdkStream({ api: "/api/chat" }),
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Native">

```tsx title="components/MyAssistant.tsx"
import { AssistantRuntimeProvider } from "@assistant-ui/react-native";
import {
  useAdkRuntime,
  createAdkStream,
} from "@assistant-ui/react-google-adk";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";

const API_URL = process.env.EXPO_PUBLIC_API_URL ?? "http://localhost:3000";

export function MyAssistant() {
  const runtime = useAdkRuntime({
    stream: createAdkStream({ api: `${API_URL}/api/chat` }),
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Ink">

```tsx title="components/MyAssistant.tsx"
import { AssistantRuntimeProvider } from "@assistant-ui/react-ink";
import {
  useAdkRuntime,
  createAdkStream,
} from "@assistant-ui/react-google-adk";
import { Thread } from "./thread.js";

export function MyAssistant() {
  const runtime = useAdkRuntime({
    stream: createAdkStream({ api: "http://localhost:3000/api/chat" }),
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 使用组件

<PlatformTabs>
<Tab value="React">

```tsx title="app/page.tsx"
import { MyAssistant } from "@/components/MyAssistant";

export default function Home() {
  return (
    <main className="h-dvh">
      <MyAssistant />
    </main>
  );
}
```

</Tab>
<Tab value="React Native">

```tsx title="app/index.tsx"
import { View } from "react-native";
import { MyAssistant } from "@/components/MyAssistant";

export default function Home() {
  return (
    <View style={{ flex: 1 }}>
      <MyAssistant />
    </View>
  );
}
```

</Tab>
<Tab value="React Ink">

```tsx title="app.tsx"
import { Box } from "ink";
import { MyAssistant } from "./components/MyAssistant.js";

export function App() {
  return (
    <Box flexDirection="column">
      <MyAssistant />
    </Box>
  );
}
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 设置 UI 组件

<PlatformTabs>
<Tab value="React">

请参阅 [UI 组件指南](/elements/thread)，了解如何连接 Thread、输入框和支持性的原语。

</Tab>
<Tab value="React Native">

请参阅 [React Native 设置指南](/docs/react-native)，了解如何添加原生 Thread、输入框和支持性的原语。

</Tab>
<Tab value="React Ink">

请参阅 [Ink 设置指南](/docs/ink)，了解如何添加终端 Thread、输入框和支持性的原语。

</Tab>
</PlatformTabs>

</Step>
</Steps>

## 添加适配器

附件、语音、反馈、历史记录和自定义线程列表均可通过标准适配器插槽实现。请参阅[适配器](/docs/runtimes/concepts/adapters)：

```tsx
const runtime = useAdkRuntime({
  stream: createAdkStream({ api: "/api/chat" }),
  adapters: { attachments, history, speech, feedback },
});
```

如需使用多线程，请参阅[线程](/docs/runtimes/concepts/threads)。

## 下一步

<Cards>
  <Card
    title="API 参考"
    description="createAdkStream、服务器辅助函数、会话适配器、线程、消息编辑。"
    href="/docs/runtimes/google-adk/api"
  />
  <Card
    title="Hooks"
    description="工具确认、身份验证、输入请求、工件、升级处理、元数据。"
    href="/docs/runtimes/google-adk/hooks"
  />
</Cards>
