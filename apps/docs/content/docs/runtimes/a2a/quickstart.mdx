---
title: 快速开始
description: 针对 A2A 服务器的最小运行时和线程设置。
---

只需三步，即可与 A2A 服务器建立可用的聊天。本文假设你已经安装了软件包，并且已有可访问的 A2A v1.0 服务器；如果没有，请从[概览](/docs/runtimes/a2a/overview)开始。

<Steps>
<Step>

### 接入运行时提供程序

<PlatformTabs>
<Tab value="React">

```tsx title="app/MyRuntimeProvider.tsx"
"use client";

import { AssistantRuntimeProvider } from "@assistant-ui/react";
import { useA2ARuntime } from "@assistant-ui/react-a2a";

export function MyRuntimeProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  const runtime = useA2ARuntime({
    baseUrl: "http://localhost:9999",
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      {children}
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Native">

```tsx title="runtime/MyRuntimeProvider.tsx"
import { AssistantRuntimeProvider } from "@assistant-ui/react-native";
import { useA2ARuntime } from "@assistant-ui/react-a2a";

export function MyRuntimeProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  const runtime = useA2ARuntime({
    baseUrl: "http://localhost:9999",
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      {children}
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Ink">

```tsx title="runtime/MyRuntimeProvider.tsx"
import { AssistantRuntimeProvider } from "@assistant-ui/react-ink";
import { useA2ARuntime } from "@assistant-ui/react-a2a";

export function MyRuntimeProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  const runtime = useA2ARuntime({
    baseUrl: "http://localhost:9999",
  });
  return (
    <AssistantRuntimeProvider runtime={runtime}>
      {children}
    </AssistantRuntimeProvider>
  );
}
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 渲染线程

<PlatformTabs>
<Tab value="React">

```tsx title="app/page.tsx"
import { Thread } from "@/components/assistant-ui/elements/thread.aui";
import { MyRuntimeProvider } from "./MyRuntimeProvider";

export default function Page() {
  return (
    <MyRuntimeProvider>
      <Thread />
    </MyRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Native">

```tsx title="app/index.tsx"
import { View } from "react-native";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";
import { MyRuntimeProvider } from "@/runtime/MyRuntimeProvider";

export default function Page() {
  return (
    <MyRuntimeProvider>
      <View style={{ flex: 1 }}>
        <Thread />
      </View>
    </MyRuntimeProvider>
  );
}
```

</Tab>
<Tab value="React Ink">

```tsx title="app.tsx"
import { Box } from "ink";
import { Thread } from "./components/thread.js";
import { MyRuntimeProvider } from "./runtime/MyRuntimeProvider.js";

export function App() {
  return (
    <MyRuntimeProvider>
      <Box flexDirection="column">
        <Thread />
      </Box>
    </MyRuntimeProvider>
  );
}
```

</Tab>
</PlatformTabs>

</Step>
<Step>

### 设置 UI 组件

<PlatformTabs>
<Tab value="React">

请参考 [UI 组件指南](/elements/thread)，接入线程、输入框和支持性原语。

</Tab>
<Tab value="React Native">

请参考 [React Native 设置指南](/docs/react-native)，添加原生线程、输入框和支持性原语。

</Tab>
<Tab value="React Ink">

请参考 [Ink 设置指南](/docs/ink)，添加终端线程、输入框和支持性原语。

</Tab>
</PlatformTabs>

</Step>
</Steps>

A2A 服务器可访问后，运行时会根据智能体卡片的 `capabilities.streaming` 标志协商采用流式传输还是非流式传输，并开始转发消息。

## 身份验证和请求头

当服务器需要身份验证时，传入静态或动态请求头：

```tsx
const runtime = useA2ARuntime({
  baseUrl: "http://localhost:9999",
  headers: async () => ({
    Authorization: `Bearer ${await getAccessToken()}`,
  }),
});
```

## 添加适配器

附件、语音、反馈、历史记录和自定义线程列表都可以通过标准适配器插槽进行支持。有关接口约定，请参阅[适配器](/docs/runtimes/concepts/adapters)；将它们传入 `useA2ARuntime`：

```tsx
const runtime = useA2ARuntime({
  baseUrl: "http://localhost:9999",
  adapters: { attachments, history, speech, feedback },
});
```

如需多线程，请参阅[线程](/docs/runtimes/concepts/threads)，并传入 `adapters.threadList`。

## 下一步

<Cards>
  <Card
    title="客户端和 Hooks"
    description="A2AClient、useA2ARuntime 选项、Hooks 参考、任务状态和工件。"
    href="/docs/runtimes/a2a/client-and-hooks"
  />
  <Card
    title="选择运行时"
    description="比较 A2A 与其他运行时选项。"
    href="/docs/runtimes/pick-a-runtime"
  />
</Cards>
