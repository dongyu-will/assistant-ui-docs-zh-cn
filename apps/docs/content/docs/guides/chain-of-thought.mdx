---
title: 思维链 UI
description: 在可折叠的思考手风琴中展示 AI 的推理步骤和工具调用。使用 assistant-ui 在 React 聊天中构建思维链可视化界面。
platforms: ["react"]
---

LLM 通常会连续产生推理步骤和工具调用。思维链可以将这些连续部分以视觉方式归组到一个可折叠的手风琴中，为用户提供简洁的“思考”界面。

## 概览

推理模型响应时，可能会在生成最终文本答案之前，先输出一系列推理 token 和工具调用。使用 `AuiIf` 将相邻的推理部分和工具调用归组到一个可折叠的“思考”区域中。

<Callout type="info">
`ChainOfThoughtPrimitive.Parts` 上较旧的 `ChainOfThoughtPrimitive` 属性，以及 `ChainOfThought` 上的 `MessagePrimitive.GroupedParts` 属性，属于旧版 API。它们仍可用于现有代码，但新代码应使用 `MessagePrimitive.GroupedParts`。
</Callout>

## 快速开始

<Steps>
  <Step>

### 将 GroupedParts 接入助手消息

为推理和工具调用返回相同的顶层分组，并为每种类型嵌套分组：

```tsx
import {
  MessagePrimitive,
  groupPartByType,
} from "@assistant-ui/react";
import { MarkdownText } from "@/components/assistant-ui/elements/markdown-text";
import {
  Reasoning,
  ReasoningContent,
  ReasoningRoot,
  ReasoningText,
  ReasoningTrigger,
} from "@/components/assistant-ui/elements/reasoning.aui";
import { ToolFallback } from "@/components/assistant-ui/elements/tool-fallback.aui";
import {
  ToolGroupContent,
  ToolGroupRoot,
  ToolGroupTrigger,
} from "@/components/assistant-ui/elements/tool-group.aui";
import type { FC } from "react";

const AssistantMessage: FC = () => {
  return (
    <MessagePrimitive.Root>
      <MessagePrimitive.GroupedParts
        groupBy={groupPartByType({
          reasoning: ["group-chainOfThought", "group-reasoning"],
          "tool-call": ["group-chainOfThought", "group-tool"],
        })}
      >
        {({ part, children }) => {
          switch (part.type) {
            case "group-chainOfThought":
              return <div className="my-2">{children}</div>;
            case "group-reasoning": {
              const running = part.status.type === "running";
              return (
                <ReasoningRoot streaming={running}>
                  <ReasoningTrigger active={running} />
                  <ReasoningContent aria-busy={running}>
                    <ReasoningText>{children}</ReasoningText>
                  </ReasoningContent>
                </ReasoningRoot>
              );
            }
            case "group-tool":
              return (
                <ToolGroupRoot>
                  <ToolGroupTrigger
                    count={part.indices.length}
                    active={part.status.type === "running"}
                  />
                  <ToolGroupContent>{children}</ToolGroupContent>
                </ToolGroupRoot>
              );
            case "text":
              return <MarkdownText />;
            case "reasoning":
              return <Reasoning {...part} />;
            case "tool-call":
              return part.toolUI ?? <ToolFallback {...part} />;
            default:
              return null;
          }
        }}
      </MessagePrimitive.GroupedParts>
    </MessagePrimitive.Root>
  );
};
```

  </Step>
  <Step>

### 使用推理模型

思维链最适合与能够生成推理 token 的模型搭配使用。下面是一个使用 AI SDK 的后端路由示例：

```tsx title="app/api/chat/route.ts"
import { openai } from "@ai-sdk/openai";
import { streamText, convertToModelMessages } from "ai";

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = streamText({
    model: openai("gpt-5.6-luna"),
    messages: await convertToModelMessages(messages),
  });

  return result.toUIMessageStreamResponse();
}
```

  </Step>
</Steps>

## LangGraph

思维链部分由 AI SDK 内置的推理流提供。LangGraph 不会以该格式输出推理 token，因此推理分组不会自动启用。如果你希望显示 LangGraph agent 的推理文本，请从图中将其作为自定义数据部分发出，并使用 `MessagePrimitive.Parts` 进行渲染。详情请参阅[使用 LangGraph 的生成式 UI](/docs/runtimes/langgraph/generative-ui)。

## 旧版：ChainOfThoughtPrimitive

### 读取折叠状态

对于现有的 `components.ChainOfThought` 代码，使用 `components` 根据手风琴状态有条件地进行渲染：

```tsx
import { AuiIf, ChainOfThoughtPrimitive } from "@assistant-ui/react";
import { ChevronDownIcon, ChevronRightIcon } from "lucide-react";

const ChainOfThoughtAccordionTrigger = () => {
  return (
    <ChainOfThoughtPrimitive.AccordionTrigger className="flex w-full cursor-pointer items-center gap-2 px-4 py-2 text-sm">
      <AuiIf condition={(s) => s.chainOfThought.collapsed}>
        <ChevronRightIcon className="size-4" />
      </AuiIf>
      <AuiIf condition={(s) => !s.chainOfThought.collapsed}>
        <ChevronDownIcon className="size-4" />
      </AuiIf>
      Thinking
    </ChainOfThoughtPrimitive.AccordionTrigger>
  );
};
```

### API 参考

有关较低层级的旧版兼容性详情，请参阅[`makeAssistantDataUI` 原语参考](/docs/primitives/chain-of-thought)。

## 完整示例

请参阅完整的 [with-chain-of-thought 示例](https://github.com/assistant-ui/assistant-ui/tree/main/examples/with-chain-of-thought)

## 相关指南

- [推理](/elements/reasoning) — 用于对部分进行分组的推理 UI 原语
- [生成式 UI](/docs/tools/tool-ui) — 用于工具调用的自定义 UI
- [工具](/docs/tools/defining-tools) — 定义和使用工具
