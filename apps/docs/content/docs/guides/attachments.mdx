---
title: 文件附件
description: 在 React 中让用户将图像、PDF 和其他文件附加到 AI 聊天消息中。assistant-ui 内置拖放、粘贴和视觉模型支持。
platforms: ["react"]
---

import { AttachmentSample } from "@/components/pages/docs/samples/attachment";

允许用户将文件附加到消息中，通过图像、文档和其他内容丰富对话。

<AttachmentSample />

## 概述

assistant-ui 中的附件系统为 AI 聊天界面中的文件上传提供了灵活的处理框架，包括：

- **附件适配器**：处理附件文件的后端逻辑
- **UI 组件**：用于显示和交互的预构建组件
- **运行时集成**：与所有 assistant-ui 运行时无缝集成

## 入门

<Steps>
  <Step>

### 安装 UI 组件

首先，将附件 UI 组件添加到项目中：

<InstallCommand shadcn={["attachment"]} />

这会将 `/components/assistant-ui/elements/attachment.tsx` 添加到项目中。

<Callout type="tip">
  **后续步骤：**你可以根据应用的设计系统，自由调整这些自动生成的组件（样式、布局和行为）。
</Callout>

  </Step>
  <Step>

### 设置运行时（无需配置）

对于 `useChatRuntime`，附件无需额外配置即可自动运行：

```tsx title="/app/MyRuntimeProvider.tsx"
import { useChatRuntime } from "@assistant-ui/ai-sdk";

const runtime = useChatRuntime();
```

<Callout type="info">
  **注意：**AI SDK 运行时会自动处理附件。对于 `useLocalRuntime` 等其他运行时，你可能仍需按照下面[创建自定义附件适配器](#creating-custom-attachment-adapters)部分中的说明配置附件适配器。
</Callout>

  </Step>
  <Step>

### 添加 UI 组件

将附件组件集成到聊天界面中。完整的安装和使用指南请参阅[附件 UI 组件](/elements/attachment)。

  </Step>
</Steps>

## 内置附件适配器

### AI SDK 运行时（默认）

使用 `useChatRuntime` 时，内置适配器接受所有文件类型，并将其转换为 base64 数据 URL。这对于图像和小型文件效果良好。

<Callout type="warn">
  大多数模型仅支持 **图像**附件。发送不受支持的文件类型（音频、视频、PDF 等）会导致 API 错误。请查看模型提供商的文档，了解其支持的输入类型。
</Callout>

如需限制接受的文件类型，请传入自定义适配器：

```tsx
const runtime = useChatRuntime({
  adapters: {
    attachments: new SimpleImageAttachmentAdapter(), // only images
  },
});
```

### SimpleImageAttachmentAdapter

处理图像文件，并将其转换为数据 URL，以便在聊天 UI 中显示。

```tsx
const imageAdapter = new SimpleImageAttachmentAdapter();
// Accepts: image/* (JPEG, PNG, GIF, etc.)
```

### SimpleTextAttachmentAdapter

处理文本文件，并将内容包装在格式化标签中：

```tsx
const textAdapter = new SimpleTextAttachmentAdapter();
// Accepts: text/plain, text/html, text/markdown, etc.
```

### CompositeAttachmentAdapter

组合多个适配器，以支持各种文件类型：

```tsx
const compositeAdapter = new CompositeAttachmentAdapter([
  new SimpleImageAttachmentAdapter(),
  new SimpleTextAttachmentAdapter(),
]);
```

## 创建自定义附件适配器

为专用文件处理场景构建自己的适配器。下面提供了常见用例的完整示例。有关 `PendingAttachment` 和 `CompleteAttachment` 类型定义，请参阅[附件类型](/elements/attachment#attachment-types)。

### 支持视觉模型的图像适配器

将图像发送给支持视觉能力的 LLM，例如 GPT-5.6、Claude Sonnet 4.6 或 Gemini Pro Vision：

```tsx
import {
  AttachmentAdapter,
  PendingAttachment,
  CompleteAttachment,
} from "@assistant-ui/react";

class VisionImageAdapter implements AttachmentAdapter {
  accept = "image/jpeg,image/png,image/webp,image/gif";

  async add({ file }: { file: File }): Promise<PendingAttachment> {
    // Validate file size (e.g., 20MB limit for most LLMs)
    const maxSize = 20 * 1024 * 1024; // 20MB
    if (file.size > maxSize) {
      throw new Error("Image size exceeds 20MB limit");
    }

    // Return pending attachment while processing
    return {
      id: crypto.randomUUID(),
      type: "image",
      name: file.name,
      file,
      status: { type: "requires-action", reason: "composer-send" },
    };
  }

  async send(attachment: PendingAttachment): Promise<CompleteAttachment> {
    // Convert image to base64 data URL
    const base64 = await this.fileToBase64DataURL(attachment.file);

    // Return in assistant-ui format with image content
    return {
      id: attachment.id,
      type: "image",
      name: attachment.name,
      content: [
        {
          type: "image",
          image: base64, // data:image/jpeg;base64,... format
        },
      ],
      status: { type: "complete" },
    };
  }

  async remove(attachment: PendingAttachment): Promise<void> {
    // Cleanup if needed (e.g., revoke object URLs if you created any)
  }

  private async fileToBase64DataURL(file: File): Promise<string> {
    return new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.onload = () => {
        // FileReader result is already a data URL
        resolve(reader.result as string);
      };
      reader.onerror = reject;
      reader.readAsDataURL(file);
    });
  }
}
```

### PDF 文档适配器

通过提取文本或将 PDF 转换为 base64 来处理 PDF 文件：

```tsx
import {
  AttachmentAdapter,
  PendingAttachment,
  CompleteAttachment,
} from "@assistant-ui/react";

class PDFAttachmentAdapter implements AttachmentAdapter {
  accept = "application/pdf";

  async add({ file }: { file: File }): Promise<PendingAttachment> {
    // Validate file size
    const maxSize = 10 * 1024 * 1024; // 10MB limit
    if (file.size > maxSize) {
      throw new Error("PDF size exceeds 10MB limit");
    }

    return {
      id: crypto.randomUUID(),
      type: "document",
      name: file.name,
      file,
      status: { type: "requires-action", reason: "composer-send" },
    };
  }

  async send(attachment: PendingAttachment): Promise<CompleteAttachment> {
    // Option 1: Extract text from PDF (requires pdf parsing library)
    // const text = await this.extractTextFromPDF(attachment.file);

    // Option 2: Convert to base64 for API processing
    const base64Data = await this.fileToBase64(attachment.file);

    return {
      id: attachment.id,
      type: "document",
      name: attachment.name,
      content: [
        {
          type: "text",
          text: `[PDF Document: ${attachment.name}]\nBase64 data: ${base64Data.substring(0, 50)}...`,
        },
      ],
      status: { type: "complete" },
    };
  }

  async remove(attachment: PendingAttachment): Promise<void> {
    // Cleanup if needed
  }

  private async fileToBase64(file: File): Promise<string> {
    const arrayBuffer = await file.arrayBuffer();
    const bytes = new Uint8Array(arrayBuffer);
    let binary = "";
    bytes.forEach((byte) => {
      binary += String.fromCharCode(byte);
    });
    return btoa(binary);
  }

  // Optional: Extract text from PDF using a library like pdf.js
  private async extractTextFromPDF(file: File): Promise<string> {
    // Implementation would use pdf.js or similar
    // This is a placeholder
    return "Extracted PDF text content";
  }
}
```

## 使用自定义适配器

### 与 LocalRuntime 配合使用

使用 `LocalRuntime` 时，需要在 `ChatModelAdapter` 中处理图像（连接 AI 后端的适配器）：

```tsx
import { useLocalRuntime, ChatModelAdapter } from "@assistant-ui/react";

// This adapter connects LocalRuntime to your AI backend
const MyModelAdapter: ChatModelAdapter = {
  async run({ messages, abortSignal }) {
    // Convert messages to format expected by your vision-capable API
    const formattedMessages = messages.map((msg) => {
      if (
        msg.role === "user" &&
        msg.content.some((part) => part.type === "image")
      ) {
        // Format for GPT-5.6 or similar vision models
        return {
          role: "user",
          content: msg.content.map((part) => {
            if (part.type === "text") {
              return { type: "text", text: part.text };
            }
            if (part.type === "image") {
              return {
                type: "image_url",
                image_url: { url: part.image },
              };
            }
            return part;
          }),
        };
      }

      // Regular text messages
      return {
        role: msg.role,
        content: msg.content
          .filter((c) => c.type === "text")
          .map((c) => c.text)
          .join("\n"),
      };
    });

    // Send to your vision-capable API
    const response = await fetch("/api/vision-chat", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ messages: formattedMessages }),
      signal: abortSignal,
    });

    const data = await response.json();
    return {
      content: [{ type: "text", text: data.message }],
    };
  },
};

// Create runtime with vision image adapter
const runtime = useLocalRuntime(MyModelAdapter, {
  adapters: {
    attachments: new VisionImageAdapter(),
  },
});
```

## 高级功能

### 进度更新

使用异步生成器提供实时上传进度：

```tsx
class UploadAttachmentAdapter implements AttachmentAdapter {
  accept = "*";

  async *add({ file }: { file: File }) {
    const id = generateId();

    // Initial pending state
    yield {
      id,
      type: "file",
      name: file.name,
      file,
      status: { type: "running", reason: "uploading", progress: 0 },
    } as PendingAttachment;

    // Simulate upload progress
    for (let progress = 10; progress <= 90; progress += 10) {
      await new Promise((resolve) => setTimeout(resolve, 100));

      yield {
        id,
        type: "file",
        name: file.name,
        file,
        status: { type: "running", reason: "uploading", progress },
      } as PendingAttachment;
    }

    // Yield final progress so the 100% state reaches the composer
    yield {
      id,
      type: "file",
      name: file.name,
      file,
      status: { type: "running", reason: "uploading", progress: 100 },
    } as PendingAttachment;
  }

  async send(attachment: PendingAttachment): Promise<CompleteAttachment> {
    // Upload the file and return complete attachment
    const url = await this.uploadFile(attachment.file);

    return {
      id: attachment.id,
      type: attachment.type,
      name: attachment.name,
      content: [
        {
          type: "file",
          data: url, // or base64 data
          mimeType: attachment.file.type,
        },
      ],
      status: { type: "complete" },
    };
  }

  async remove(attachment: PendingAttachment): Promise<void> {
    // Cleanup logic
  }

  private async uploadFile(file: File): Promise<string> {
    // Your upload logic here
    return "https://example.com/file-url";
  }
}
```

### 验证与错误处理

在适配器中实现完善的验证：

```tsx
class ValidatedImageAdapter implements AttachmentAdapter {
  accept = "image/*";
  maxSizeBytes = 5 * 1024 * 1024; // 5MB

  async add({ file }: { file: File }): Promise<PendingAttachment> {
    // Validate file size
    if (file.size > this.maxSizeBytes) {
      return {
        id: generateId(),
        type: "image",
        name: file.name,
        file,
        status: {
          type: "incomplete",
          reason: "error",
        },
      };
    }

    // Validate image dimensions
    try {
      const dimensions = await this.getImageDimensions(file);
      if (dimensions.width > 4096 || dimensions.height > 4096) {
        throw new Error("Image dimensions exceed 4096x4096");
      }
    } catch (error) {
      return {
        id: generateId(),
        type: "image",
        name: file.name,
        file,
        status: {
          type: "incomplete",
          reason: "error",
        },
      };
    }

    // Return valid attachment
    return {
      id: generateId(),
      type: "image",
      name: file.name,
      file,
      status: { type: "requires-action", reason: "composer-send" },
    };
  }

  private async getImageDimensions(file: File) {
    // Implementation to check image dimensions
  }
}
```

如需在 UI 中显示失败信息，请订阅 `composer.attachmentAddError`。每当添加操作失败时，该事件都会触发，具体有以下两种情况：

1. `addAttachment()` 被拒绝：未配置适配器、文件类型与 `accept` 不匹配，或适配器的 `add()` 抛出异常。
2. `addAttachment()` 成功解析，但适配器返回的（或异步迭代器适配器生成的）附件的 `status.reason === "error"`。虽然 promise 成功解析，但事件仍会触发，以便 UI 做出响应。

事件负载包含一个 `reason` 判别字段和人类可读的 `message`，因此你可以根据失败模式分支处理 UI：

| `reason`         | 触发时机                                                                                          |
| ---------------- | ------------------------------------------------------------------------------------------------------ |
| `no-adapter`     | 已调用 `addAttachment(File)`，但未配置 `AttachmentAdapter`。                             |
| `not-accepted`   | 文件的内容类型（或文件名扩展名）与 `adapter.accept` 不匹配。当外部 `CreateAttachment` 描述符的 `contentType` 与 `adapter.accept` 不匹配时，也会触发此事件。 |
| `adapter-error`  | 适配器的 `add()` 抛出异常，或返回/生成了带有 `status.reason === "error"` 的附件。如果适配器在失败前生成了任何附件，出错的附件也会显示在 `composer.attachments` 中；如果它在生成附件前就抛出异常，则该事件是唯一信号。 |

```tsx
import { toast } from "sonner"; // or your toast library of choice
import { useAuiEvent } from "@assistant-ui/react";

function AttachmentErrorToast() {
  useAuiEvent("composer.attachmentAddError", ({ reason, message, error }) => {
    if (reason === "not-accepted") {
      toast.error("This file type is not supported.");
    } else if (reason === "no-adapter") {
      toast.error("Attachments are not configured for this composer.");
    } else {
      if (error) console.error(error); // underlying Error, useful for logging
      toast.error(message || "Attachment failed to upload.");
    }
  });
  return null;
}
```

当失败与已注册的附件相关时，会包含 `attachmentId`（通常是 `adapter-error` 情况）。对于 `no-adapter` 和 `not-accepted` 失败，该字段为 `undefined`，因为这些操作会在注册任何附件之前被拒绝。

### 外部来源附件

无需 `File` 对象或 `AttachmentAdapter`，即可从外部来源（URL、API 数据、CMS 引用）添加附件：

```tsx
const aui = useAui();

// Add an attachment from an external source
await aui.composer.addAttachment({
  name: "report.pdf",
  contentType: "application/pdf",
  content: [{ type: "text", text: "Extracted document content..." }],
});

// Optionally provide id and type
await aui.composer.addAttachment({
  id: "cms-doc-123",
  type: "document",
  name: "Product Spec",
  content: [{ type: "text", text: "Product specification content..." }],
});
```

外部附件会直接作为完整附件添加。它们会跳过 `AttachmentAdapter` 的 `add()` 步骤（无需上传），但在配置了 `AttachmentAdapter` 时仍会执行 `adapter.accept`：`contentType` 与 `adapter.accept` 不匹配的 `CreateAttachment` 会被拒绝，并触发 `composer.attachmentAddError`。如果省略 `contentType`，只有当 `accept` 本身包含明确的扩展名条目（例如 `.png,.pdf`）时，才会将描述符的文件名扩展名与 `adapter.accept` 进行匹配；对于 `image/*` 等 MIME 通配符 `accept` 字符串，则始终需要匹配的 `contentType`。未配置 `AttachmentAdapter` 时，外部附件会在不检查内容类型的情况下添加，并且无需适配器即可移除。

### 多文件选择

使用自定义限制启用多文件选择：

```tsx
const aui = useAui();

const handleMultipleFiles = async (files: FileList) => {
  const maxFiles = 5;
  const filesToAdd = Array.from(files).slice(0, maxFiles);

  for (const file of filesToAdd) {
    await aui.composer.addAttachment(file);
  }
};
```

## 后端集成

### 与 Vercel AI SDK 配合使用

附件会以文件内容部分的形式发送到后端。

## 运行时支持

附件适用于所有 assistant-ui 运行时：

- **AI SDK Runtime**：`useChatRuntime`
- **External Store**：`useExternalStoreRuntime`
- **LangGraph**：`useLangGraphRuntime`
- **自定义运行时**：任何实现附件接口的运行时

<Callout type="tip">
  附件系统旨在实现可扩展性。你可以为任何文件类型创建适配器、
  集成云存储服务，或实现自定义处理逻辑，以满足具体需求。
</Callout>

## 大文件上传

内置适配器会在内存中将文件转换为 base64 数据 URL。对于大文件（长音频、视频等），这可能导致性能问题。此时应改为将文件上传到服务器，并传递 URL：

```tsx
class ServerUploadAdapter implements AttachmentAdapter {
  accept = "*";
  private urls = new Map<string, string>();

  async *add({ file }: { file: File }) {
    const id = crypto.randomUUID();
    yield {
      id, type: "file" as const, name: file.name, file,
      contentType: file.type,
      status: { type: "running" as const, reason: "uploading" as const, progress: 0 },
    };

    const form = new FormData();
    form.append("file", file);
    const { url } = await fetch("/api/upload", { method: "POST", body: form }).then(r => r.json());
    this.urls.set(id, url);

    yield {
      id, type: "file" as const, name: file.name, file,
      contentType: file.type,
      status: { type: "requires-action" as const, reason: "composer-send" as const },
    };
  }

  async send(attachment: PendingAttachment): Promise<CompleteAttachment> {
    const url = this.urls.get(attachment.id)!;
    this.urls.delete(attachment.id);
    return {
      ...attachment, status: { type: "complete" },
      content: [{ type: "file", data: url, mimeType: attachment.contentType ?? "", filename: attachment.name }],
    };
  }

  async remove() {}
}
```

<Callout type="tip">
  [assistant-ui Cloud](/docs/cloud/ai-sdk-assistant-ui) 包含 `CloudFileAttachmentAdapter`，可通过预签名 URL 开箱即用地处理大文件上传。
</Callout>

## 最佳实践

1. **文件大小限制**：始终验证文件大小，以避免内存问题
2. **类型验证**：确认文件类型符合你的 `accept` 模式
3. **错误处理**：为失败的上传提供清晰的错误消息
4. **进度反馈**：显示上传进度，提升用户体验
5. **安全性**：处理文件内容前进行验证和清理
6. **无障碍性**：确保附件 UI 支持键盘操作

## 资源

- [附件 UI 组件](/elements/attachment) - UI 实现详情
- [API 参考](/docs/api-reference/overview) - 详细的类型定义
