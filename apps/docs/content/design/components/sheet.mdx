---
title: "Sheet"
description: "一个从边缘滑入、用于次级流程的面板。"
links:
  - label: 源代码
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/sheet.tsx
  - label: Base UI Dialog
    url: https://base-ui.com/react/components/dialog
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  SheetSpecimen,
  SheetSidesSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="SheetSpecimen" base={<SheetSpecimen />}>
  <RadixSpecimens.SheetSpecimen />
</PreviewCode>

## 安装

<KitInstall name="sheet" />

## 用法

```tsx
import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet";
import { Button } from "@/components/ui/button";

export function Example() {
  return (
    <Sheet>
      <SheetTrigger render={<Button variant="outline">Open threads</Button>} />
      <SheetContent side="right">
        <SheetHeader>
          <SheetTitle>Threads</SheetTitle>
          <SheetDescription>Recent conversations.</SheetDescription>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  );
}
```

## 示例

### 侧边

assistant-ui 示例中的移动端线程列表使用的是 `left` Sheet。

<PreviewCode file="components/pages/design/specimens" name="SheetSidesSpecimen" base={<SheetSidesSpecimen />}>
  <RadixSpecimens.SheetSidesSpecimen />
</PreviewCode>

## API 参考

这些部件封装了 Base UI 的 Dialog 原语，并接受其 props。

### SheetContent

<ParametersTable
  type="SheetContentProps"
  parameters={[
    {
      name: "side",
      type: '"top" | "right" | "bottom" | "left"',
      default: '"right"',
      description: "面板从哪个边缘进入。",
    },
    {
      name: "showCloseButton",
      type: "boolean",
      default: "true",
      description: "渲染角落关闭控件。",
    },
  ]}
/>
