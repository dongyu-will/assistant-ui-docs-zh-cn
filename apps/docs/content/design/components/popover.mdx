---
title: "Popover"
description: "按需显示锚定详情，点击其他区域即可关闭。"
links:
  - label: 来源
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/popover.tsx
  - label: Base UI Popover
    url: https://base-ui.com/react/components/popover
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  PopoverSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="PopoverSpecimen" base={<PopoverSpecimen />}>
  <RadixSpecimens.PopoverSpecimen />
</PreviewCode>

## 安装

<KitInstall name="popover" />

## 使用

```tsx
import {
  Popover,
  PopoverContent,
  PopoverDescription,
  PopoverHeader,
  PopoverTitle,
  PopoverTrigger,
} from "@/components/ui/popover";
import { Button } from "@/components/ui/button";

export function Example() {
  return (
    <Popover>
      <PopoverTrigger render={<Button variant="outline">Details</Button>} />
      <PopoverContent>
        <PopoverHeader>
          <PopoverTitle>Context usage</PopoverTitle>
          <PopoverDescription>12.4k of 200k tokens.</PopoverDescription>
        </PopoverHeader>
      </PopoverContent>
    </Popover>
  );
}
```

## API 参考

这些部件封装了 Base UI 的 Popover 原语，并接受其 props。

### Popover

<ParametersTable
  type="PopoverProps"
  parameters={[
    {
      name: "open",
      type: "boolean",
      description: "受控的打开状态。",
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void",
      description: "打开状态发生变化时调用。",
    },
  ]}
/>

### PopoverContent

<ParametersTable
  type="PopoverContentProps"
  parameters={[
    {
      name: "side",
      type: '"top" | "right" | "bottom" | "left"',
      default: '"bottom"',
      description: "触发器的首选侧。",
    },
    {
      name: "align",
      type: '"start" | "center" | "end"',
      default: '"center"',
      description: "相对于触发器的对齐方式。",
    },
    {
      name: "sideOffset",
      type: "number",
      default: "4",
      description: "与触发器之间的距离，单位为像素。",
    },
  ]}
/>
