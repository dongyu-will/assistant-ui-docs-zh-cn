---
title: "Skeleton"
description: "具有已知布局的内容加载形状。"
links:
  - label: 官方原文
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/skeleton.tsx
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  SkeletonSpecimen,
  SkeletonCardSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="SkeletonSpecimen" base={<SkeletonSpecimen />}>
  <RadixSpecimens.SkeletonSpecimen />
</PreviewCode>

## 安装

<KitInstall name="skeleton" />

## 使用

```tsx
import { Skeleton } from "@/components/ui/skeleton";

export function Example() {
  return (
    <div className="flex items-center gap-3">
      <Skeleton className="size-10 rounded-full" />
      <Skeleton className="h-4 w-40" />
    </div>
  );
}
```

## 示例

### 卡片

将骨架屏块组合成数据填充后的布局；形状越接近，替换时越平滑。

<PreviewCode file="components/pages/design/specimens" name="SkeletonCardSpecimen" base={<SkeletonCardSpecimen />}>
  <RadixSpecimens.SkeletonCardSpecimen />
</PreviewCode>

## API 参考

### Skeleton

渲染一个脉动的 `div`；其尺寸完全取决于 `className`。

<ParametersTable
  type="SkeletonProps"
  parameters={[
    {
      name: "className",
      type: "string",
      description: "占位符的宽度、高度和圆角半径。",
    },
  ]}
/>
