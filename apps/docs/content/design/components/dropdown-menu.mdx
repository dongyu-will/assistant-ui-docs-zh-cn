---
title: "下拉菜单"
description: "触发器背后的操作：在安静的界面上提供项目、快捷键和分隔符。"
links:
  - label: 官方原文
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/dropdown-menu.tsx
  - label: Base UI Menu
    url: https://base-ui.com/react/components/menu
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  DropdownMenuSpecimen,
  DropdownMenuChecklistSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="DropdownMenuSpecimen" base={<DropdownMenuSpecimen />}>
  <RadixSpecimens.DropdownMenuSpecimen />
</PreviewCode>

## 安装

<KitInstall name="dropdown-menu" />

## 用法

```tsx
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { Button } from "@/components/ui/button";

export function Example() {
  return (
    <DropdownMenu>
      <DropdownMenuTrigger render={<Button variant="outline">Open</Button>} />
      <DropdownMenuContent>
        <DropdownMenuItem>Profile</DropdownMenuItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem>Sign out</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

## 示例

### 复选框和子菜单

`DropdownMenuCheckboxItem`保存选中状态，`DropdownMenuLabel`为组提供标题，而`DropdownMenuSub`嵌套第二层菜单。

<PreviewCode file="components/pages/design/specimens" name="DropdownMenuChecklistSpecimen" base={<DropdownMenuChecklistSpecimen />}>
  <RadixSpecimens.DropdownMenuChecklistSpecimen />
</PreviewCode>

## API 参考

这些部分封装了 Base UI 的 Menu 原语，并接受其 props。

### DropdownMenu

<ParametersTable
  type="DropdownMenuProps"
  parameters={[
    {
      name: "open",
      type: "boolean",
      description: "受控的打开状态。",
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void",
      description: "打开状态发生变化时调用。",
    },
    {
      name: "defaultOpen",
      type: "boolean",
      default: "false",
      description: "非受控的初始打开状态。",
    },
  ]}
/>

### DropdownMenuContent

<ParametersTable
  type="DropdownMenuContentProps"
  parameters={[
    {
      name: "align",
      type: '"start" | "center" | "end"',
      default: '"center"',
      description: "相对于触发器的对齐方式。",
    },
    {
      name: "sideOffset",
      type: "number",
      default: "4",
      description: "与触发器之间的像素距离。",
    },
  ]}
/>

### DropdownMenuItem

<ParametersTable
  type="DropdownMenuItemProps"
  parameters={[
    {
      name: "onSelect",
      type: "() => void",
      description: "项目被选中时调用。",
    },
    {
      name: "disabled",
      type: "boolean",
      default: "false",
      description: "阻止选择并弱化项目。",
    },
  ]}
/>
