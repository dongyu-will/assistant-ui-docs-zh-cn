---
title: "Dialog"
description: "用于需要页面等待用户做出决定的模态面板。"
links:
  - label: 官方原文
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/dialog.tsx
  - label: Base UI Dialog
    url: https://base-ui.com/react/components/dialog
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  DialogSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="DialogSpecimen" base={<DialogSpecimen />}>
  <RadixSpecimens.DialogSpecimen />
</PreviewCode>

## 安装

<KitInstall name="dialog" />

## 使用

```tsx
import {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";

export function Example() {
  return (
    <Dialog>
      <DialogTrigger render={<Button variant="outline">Rename</Button>} />
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Rename thread</DialogTitle>
          <DialogDescription>Visible to everyone in this workspace.</DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <DialogClose render={<Button variant="ghost">Cancel</Button>} />
          <DialogClose render={<Button>Save</Button>} />
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

## API 参考

这些部件封装了 Base UI 的 Dialog 原语，并接受其 props。

### Dialog

<ParametersTable
  type="DialogProps"
  parameters={[
    {
      name: "open",
      type: "boolean",
      description: "受控的打开状态。",
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void",
      description: "打开状态发生变化时调用，包括按下 Escape 键和点击背景。",
    },
    {
      name: "defaultOpen",
      type: "boolean",
      default: "false",
      description: "非受控的初始打开状态。",
    },
  ]}
/>

### 部件

| 部件 | 描述 |
|------|-------------|
| `DialogTrigger` | 打开对话框；将控件与 `render` 组合使用。 |
| `DialogContent` | 位于中央、采用对话框圆角的面板。 |
| `DialogHeader` | 标题和描述的垂直堆叠区域。 |
| `DialogFooter` | 右对齐的操作行。 |
| `DialogClose` | 包装任何应关闭对话框的控件。 |
