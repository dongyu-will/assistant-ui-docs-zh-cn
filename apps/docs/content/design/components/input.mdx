---
title: "输入框"
description: "带有标签的单行字段，以细线绘制。"
links:
  - label: 原文
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/ui/base/input.tsx
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  InputSpecimen,
  InputWithButtonSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="InputSpecimen" base={<InputSpecimen />}>
  <RadixSpecimens.InputSpecimen />
</PreviewCode>

## 安装

<KitInstall name="input" />

## 用法

```tsx
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function Example() {
  return (
    <div className="flex flex-col gap-2">
      <Label htmlFor="email">Email</Label>
      <Input id="email" type="email" placeholder="you@example.com" />
    </div>
  );
}
```

## 示例

### 与按钮搭配

输入框与按钮采用相同的 32px 节奏，因此无需调整对齐方式即可组合成行。

<PreviewCode file="components/pages/design/specimens" name="InputWithButtonSpecimen" base={<InputWithButtonSpecimen />}>
  <RadixSpecimens.InputWithButtonSpecimen />
</PreviewCode>

## API 参考

### Input

接受所有原生 `input` 属性。

<ParametersTable
  type="InputProps"
  parameters={[
    {
      name: "type",
      type: "string",
      default: '"text"',
      description: "原生输入框类型。",
    },
    {
      name: "disabled",
      type: "boolean",
      default: "false",
      description: "阻止编辑并弱化字段显示。",
    },
    {
      name: "className",
      type: "string",
      description: "其他 CSS 类。",
    },
  ]}
/>
