---
title: Claude 克隆版
description: 使用 React 构建的开源 Claude 克隆版——采用暖奶油色主题、衬线字体、仅悬停时显示的操作栏，以及仿照 claude.ai 设计的简洁极简阴影输入框。
---

## 概览

Claude 克隆版展示了如何自定义 assistant-ui，使其匹配 Anthropic 的 Claude 界面。设计采用 Claude 标志性的暖奶油色背景、衬线字体和刻意保持极简的界面装饰——没有阴影，没有头像，只有输入框上的细边框，以及融入页面的气泡。

## 功能

- **暖奶油色背景**：`#F0ECE0` 浅色，`#2b2a27` 深色——更偏暖色的 Anthropic 风格奶油色
- **全局衬线字体**：所有界面都使用 `font-serif`，营造编辑风格
- **Sparkle 标题**：居中的“今天我能帮你做什么？”以及橙色的 `#c96442` Sparkle 图标
- **无边框输入框**：仅使用 `border-[#E5E0D6]`，不带阴影——契合 claude.ai 的极简外观
- **可用的模型选择器**：包含 Sonnet 4.5 / Opus 4.7 / Haiku 4.5 的下拉菜单，并提供“更多选项”
- **模式标签页**：写作 / 学习 / 代码 / 来自云端硬盘 / 来自日历——带细边框的 `rounded-lg` 标签
- **仅悬停时显示的操作栏**：用户的编辑/复制和助手的复制/👍/👎/重新加载操作仅在悬停时显示
- **四状态主要操作**：Cancel、StopDictation、Send、Dictate（橙色 `#c96442` 强调色）

## 快速开始

```bash
npx assistant-ui add thread
```

## 代码

空状态会将标题、输入框和模式标签页组合并居中显示。聊天状态使用粘性输入框，并在奶油色背景上叠加淡出渐变：

```tsx
import {
  AuiIf,
  ThreadPrimitive,
  ComposerPrimitive,
  MessagePrimitive,
  ActionBarPrimitive,
} from "@assistant-ui/react";

export const Claude = () => (
  <ThreadPrimitive.Root className="bg-[#F0ECE0] font-serif dark:bg-[#2b2a27]">
    <AuiIf condition={(s) => s.thread.isEmpty}>
      <EmptyState />
    </AuiIf>
    <AuiIf condition={(s) => !s.thread.isEmpty}>
      <ThreadPrimitive.Viewport>
        <ThreadPrimitive.Messages>
          {() => <ChatMessage />}
        </ThreadPrimitive.Messages>
        <ThreadPrimitive.ViewportFooter className="sticky bottom-0">
          <Composer />
          <p>Claude can make mistakes. Please double-check responses.</p>
        </ThreadPrimitive.ViewportFooter>
      </ThreadPrimitive.Viewport>
    </AuiIf>
  </ThreadPrimitive.Root>
);

const EmptyState = () => (
  <div className="flex grow flex-col items-center justify-center">
    <h1 className="flex items-center gap-3 font-serif text-3xl">
      <Sparkle className="fill-[#c96442] text-[#c96442]" />
      How can I help you today?
    </h1>
    <Composer />
    <ModeTabs />
  </div>
);

const Composer = () => (
  <ComposerPrimitive.Root className="rounded-2xl border border-[#E5E0D6] bg-white px-3.5 pt-3 pb-2.5 dark:border-[#3d3a35] dark:bg-[#1f1e1b]">
    <ComposerPrimitive.Input rows={1} placeholder="How can I help you today?" />
    <div className="flex items-center gap-2">
      <ComposerPrimitive.AddAttachment />
      <div className="ml-auto flex items-center gap-1">
        <ModelPicker />
        <PrimaryAction />
      </div>
    </div>
  </ComposerPrimitive.Root>
);
```

### 仅悬停时显示的操作栏

用户和助手的操作栏都使用不透明度与 group-hover，避免干扰界面：

```tsx
<MessagePrimitive.Root className="group/message">
  <div className="rounded-2xl bg-[#E5E0D6] px-4 py-2.5">
    <MessagePrimitive.Parts components={{ Text: MarkdownText }} />
  </div>
  <ActionBarPrimitive.Root
    hideWhenRunning
    autohide="not-last"
    className="opacity-0 transition-opacity group-hover/message:opacity-100"
  >
    <ActionBarPrimitive.Edit />
    <ActionBarPrimitive.Copy />
  </ActionBarPrimitive.Root>
</MessagePrimitive.Root>
```

### 配色

| 元素 | 浅色 | 深色 |
|---------|-------|------|
| 背景 | `#F0ECE0` | `#2b2a27` |
| 输入框表面 | `white` | `#1f1e1b` |
| 输入框边框 | `#E5E0D6` | `#3d3a35` |
| 用户气泡 | `#E5E0D6` | `#393937` |
| 主要文本 | `#1a1a18` | `#eee` |
| 次要文本 | `#5b5950` | `#a3a098` |
| 主要操作（橙色） | `#c96442` | `#c96442` |

### 样式细节

- **字体**：所有界面都使用 `font-serif`；助手消息使用 `leading-[1.65rem]`，营造舒缓的阅读节奏
- **无阴影**：输入框仅依赖 `border-[#E5E0D6]`——这是 claude.ai 标志性的扁平表面
- **用户气泡**：`rounded-2xl bg-[#E5E0D6] max-w-[80%]`，靠右对齐
- **助手消息**：全宽的普通衬线文本，没有气泡，也没有头像——文字直接显示在奶油色背景上
- **模式标签页**：使用 `rounded-lg border border-[#E5E0D6] bg-transparent`，使其自然融入奶油色背景

## 源码

<SourceLink href="https://github.com/assistant-ui/assistant-ui/blob/main/apps/docs/components/pages/examples/claude.tsx" />
