---
title: Mem0 记忆聊天
description: 由 Mem0 提供持久记忆的 AI 聊天——跨会话记住用户偏好、事实和历史记录。基于 assistant-ui 构建的开源 React 示例。
---

## 概述

这是一个由 Mem0 提供支持的个性化 AI 聊天体验，可以记住跨对话的用户偏好、事实和上下文。与每次对话都从头开始的传统聊天应用不同，此示例会持续保存用户交互记忆，从而提供越来越相关且个性化的回复。

## 功能

- **持久记忆**：事实和偏好会在不同聊天会话之间保留
- **自动提取**：AI 会自动识别并存储重要信息
- **上下文注入**：每次对话都会包含相关记忆
- **记忆管理**：用户可以查看、编辑和删除已存储的记忆
- **隐私控制**：精细控制要记住哪些内容
- **记忆可视化**：通过清晰的界面展示 AI 记住的内容

## 工作原理

1. **对话**：用户正常与 AI 聊天
2. **提取**：Mem0 识别关键事实（例如：“用户偏好深色模式”）
3. **存储**：事实存储在 Mem0 的记忆系统中
4. **检索**：在后续对话中检索相关记忆
5. **个性化**：AI 回复会结合已记住的上下文

## 集成

此示例将 Mem0 的记忆 SDK 与 assistant-ui 集成：

```tsx
import MemoryClient from "mem0ai";

const mem0 = new MemoryClient({
  apiKey: process.env.MEM0_API_KEY,
});

// Store memories from a conversation
async function storeMemory(userId: string, messages: Message[]) {
  await mem0.add(
    messages.map((m) => ({
      role: m.role,
      content: m.content,
    })),
    { user_id: userId },
  );
}

// Retrieve relevant memories for context
async function getRelevantMemories(userId: string, query: string) {
  const { results } = await mem0.search(query, {
    user_id: userId,
    limit: 10,
  });

  return results.map((m) => m.memory).join("\n");
}
```

### 在聊天中使用记忆

```tsx
// In your API route
export async function POST(req: Request) {
  const { messages, userId } = await req.json();
  const lastMessage = messages[messages.length - 1].content;

  // Get relevant memories
  const memories = await getRelevantMemories(userId, lastMessage);

  // Include memories in system prompt
  const systemPrompt = `You are a helpful assistant.

Here's what you remember about this user:
${memories}

Use this context to personalize your responses.`;

  const result = streamText({
    model: openai("gpt-5.6-luna"),
    system: systemPrompt,
    messages,
  });

  // Store new memories after response
  await storeMemory(userId, messages);

  return result.toUIMessageStreamResponse();
}
```

### 记忆类型

| 类型 | 示例 |
|------|---------|
| 偏好 | “用户偏好简洁的回答” |
| 事实 | “用户在 Acme Corp 工作” |
| 上下文 | “用户正在构建 React 应用” |
| 历史记录 | “之前讨论过身份验证” |

## 源代码

<SourceLink href="https://github.com/mem0ai/mem0/blob/main/examples/mem0-demo/components/assistant-ui/thread.tsx" />
