---
title: 浮动模态聊天
description: 基于 assistant-ui 构建的可嵌入式 AI 助手，运行于浮动按钮模态框中——可添加到任意 React 应用，为产品内副驾驶或支持聊天提供支持。
---

## 概览

Modal 示例演示了如何创建一个浮动操作按钮，在模态对话框中打开 AI 助手聊天界面。这种模式非常适合希望提供 AI 辅助、又不干扰主用户界面的应用。

## 功能

- **浮动操作按钮**：固定在屏幕角落的简洁、易访问按钮
- **模态对话框**：功能完整的聊天界面，并具备完善的焦点管理
- **流畅动画**：带有缩放和滑动效果的进入/退出过渡
- **响应式设计**：适用于桌面设备和移动设备
- **键盘导航**：支持使用 Escape 键关闭，并提供合理的 Tab 顺序

## 快速开始

```bash
npx assistant-ui add assistant-modal
```

## 代码

该模态框使用 `AssistantModalPrimitive` 创建浮动聊天界面：

```tsx
import { AssistantModalPrimitive } from "@assistant-ui/react";
import { Thread } from "@/components/assistant-ui/elements/thread.aui";
import { BotIcon } from "lucide-react";

export const AssistantModal = () => {
  return (
    <AssistantModalPrimitive.Root>
      <AssistantModalPrimitive.Anchor className="fixed right-4 bottom-4 size-11">
        <AssistantModalPrimitive.Trigger asChild>
          <button className="size-full rounded-full bg-primary shadow-lg transition-transform duration-150 ease-out hover:scale-105 active:scale-96 motion-reduce:transition-none">
            <BotIcon />
          </button>
        </AssistantModalPrimitive.Trigger>
      </AssistantModalPrimitive.Anchor>

      <AssistantModalPrimitive.Content
        sideOffset={16}
        className="h-[500px] w-[400px] origin-(--radix-popover-content-transform-origin) rounded-[2.5rem] border bg-popover shadow-xl ease-[cubic-bezier(0.32,0.72,0,1)] data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=open]:zoom-in-95 data-[state=open]:slide-in-from-bottom-2 data-[state=open]:duration-300 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95 data-[state=closed]:slide-out-to-bottom-2 data-[state=closed]:duration-200 motion-reduce:animate-none"
      >
        <Thread />
      </AssistantModalPrimitive.Content>
    </AssistantModalPrimitive.Root>
  );
};
```

### 主要组件

| 组件 | 用途 |
|-----------|---------|
| `AssistantModalPrimitive.Root` | 管理打开/关闭状态的容器 |
| `AssistantModalPrimitive.Anchor` | 定位触发按钮 |
| `AssistantModalPrimitive.Trigger` | 打开模态框的按钮 |
| `AssistantModalPrimitive.Content` | 包含聊天界面的模态对话框 |

### 自定义提示

- 调整 `sideOffset` 以控制按钮与模态框之间的间距
- 修改 `h-[500px] w-[400px]` 以更改模态框尺寸
- 使用 `data-[state=open/closed]` 设置动画状态
- 使用 `fixed right-4 bottom-4` 将锚点定位到任意角落

## 源码

<SourceLink href="https://github.com/assistant-ui/assistant-ui/blob/main/apps/docs/components/pages/docs/samples/assistant-modal.tsx" />
